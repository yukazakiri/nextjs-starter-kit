generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model account_balance {
  id            BigInt   @id(map: "idx_21439_primary") @default(autoincrement())
  student_id    BigInt   @unique(map: "idx_21439_fk_account_balance_students")
  semester      String   @db.VarChar(255)
  academic_year String   @db.VarChar(255)
  school_year   String   @db.VarChar(255)
  total_fees    Decimal  @db.Decimal(8, 2)
  amount_paid   Decimal  @db.Decimal(8, 2)
  balance       Decimal  @db.Decimal(8, 2)
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  updated_at    DateTime @default(now()) @db.Timestamptz(6)
}

model account_request_metas {
  id                 BigInt    @id(map: "idx_21458_primary") @default(autoincrement())
  user_id            BigInt?
  model_id           Decimal?  @db.Decimal
  model_type         String?   @db.VarChar(255)
  account_request_id Decimal   @db.Decimal
  key                String    @db.VarChar(255)
  value              String?
  is_approved        Boolean?  @default(false)
  is_approved_at     DateTime? @db.Timestamptz(6)
  is_rejected        Boolean?  @default(false)
  is_rejected_at     DateTime? @db.Timestamptz(6)
  rejected_reason    String?
  created_at         DateTime? @db.Timestamptz(6)
  updated_at         DateTime? @db.Timestamptz(6)

  @@index([account_request_id], map: "idx_21458_account_request_metas_account_request_id_foreign")
  @@index([key], map: "idx_21458_account_request_metas_key_index")
}

model account_requests {
  id             BigInt    @id(map: "idx_21448_primary") @default(autoincrement())
  account_id     Decimal   @db.Decimal
  user_id        BigInt?
  type           String?   @db.VarChar(255)
  status         String?   @default("pending") @db.VarChar(255)
  is_approved    Boolean?  @default(false)
  is_approved_at DateTime? @db.Timestamptz(6)
  created_at     DateTime? @db.Timestamptz(6)
  updated_at     DateTime? @db.Timestamptz(6)

  @@index([account_id], map: "idx_21448_account_requests_account_id_foreign")
}

model accounts {
  id                                                                                 BigInt                           @id(map: "idx_21390_primary") @default(autoincrement())
  type                                                                               String?                          @default("account") @db.VarChar(255)
  parent_id                                                                          Decimal?                         @db.Decimal
  name                                                                               String?                          @db.VarChar(255)
  username                                                                           String                           @unique(map: "idx_21390_accounts_username_unique") @db.VarChar(255)
  email                                                                              String?                          @db.VarChar(255)
  phone                                                                              String?                          @db.VarChar(255)
  loginby                                                                            String?                          @default("email") @db.VarChar(255)
  address                                                                            String?
  lang                                                                               String?                          @db.VarChar(10)
  password                                                                           String?                          @db.VarChar(255)
  two_factor_secret                                                                  String?
  two_factor_recovery_codes                                                          String?
  two_factor_confirmed_at                                                            DateTime?                        @db.Timestamptz(6)
  otp_code                                                                           String?                          @db.VarChar(255)
  otp_activated_at                                                                   DateTime?                        @db.Timestamptz(6)
  last_login                                                                         DateTime?                        @db.Timestamptz(6)
  agent                                                                              String?
  host                                                                               String?                          @db.VarChar(255)
  is_login                                                                           Boolean?                         @default(false)
  is_active                                                                          Boolean                          @default(false)
  is_notification_active                                                             Boolean                          @default(true)
  deleted_at                                                                         DateTime?                        @db.Timestamptz(6)
  created_at                                                                         DateTime?                        @db.Timestamptz(6)
  updated_at                                                                         DateTime?                        @db.Timestamptz(6)
  person_id                                                                          String?                          @db.VarChar(255)
  profile_photo_url                                                                  String?                          @db.VarChar(255)
  email_verified_at                                                                  String?                          @db.VarChar(255)
  role                                                                               String?                          @db.VarChar(255)
  avatar                                                                             String?                          @db.VarChar(255)
  person_type                                                                        String?                          @db.VarChar(255)
  profile_photo_path                                                                 String?                          @db.VarChar(255)
  remember_token                                                                     String?                          @db.VarChar(255)
  current_team_id                                                                    BigInt?
  teams                                                                              teams?                           @relation(fields: [current_team_id], references: [id], onUpdate: NoAction, map: "accounts_current_team_id_foreign")
  faculty_notifications                                                              faculty_notifications[]
  login_links                                                                        login_links[]
  missing_student_requests                                                           missing_student_requests[]
  notification_campaigns_notification_campaigns_created_byToaccounts                 notification_campaigns[]         @relation("notification_campaigns_created_byToaccounts")
  notification_campaigns_notification_campaigns_updated_byToaccounts                 notification_campaigns[]         @relation("notification_campaigns_updated_byToaccounts")
  oauth_connections                                                                  oauth_connections[]
  routes_routes_created_byToaccounts                                                 routes[]                         @relation("routes_created_byToaccounts")
  routes_routes_updated_byToaccounts                                                 routes[]                         @relation("routes_updated_byToaccounts")
  student_notification_campaigns_student_notification_campaigns_created_byToaccounts student_notification_campaigns[] @relation("student_notification_campaigns_created_byToaccounts")
  student_notification_campaigns_student_notification_campaigns_updated_byToaccounts student_notification_campaigns[] @relation("student_notification_campaigns_updated_byToaccounts")
  student_notifications                                                              student_notifications[]
}

model accounts_metas {
  id         BigInt    @id(map: "idx_21431_primary") @default(autoincrement())
  model_id   Decimal?  @db.Decimal
  model_type String?   @db.VarChar(255)
  account_id Decimal   @db.Decimal
  key        String    @db.VarChar(255)
  value      String?
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@index([account_id], map: "idx_21431_accounts_metas_account_id_foreign")
  @@index([key], map: "idx_21431_accounts_metas_key_index")
}

model accountsportal {
  id                        BigInt    @id(map: "idx_21416_primary") @default(autoincrement())
  email                     String    @unique(map: "idx_21416_accounts_email_unique") @db.VarChar(255)
  email_verified_at         DateTime? @db.Timestamptz(6)
  password                  String?   @db.VarChar(255)
  two_factor_secret         String?
  two_factor_recovery_codes String?
  two_factor_confirmed_at   DateTime? @db.Timestamptz(6)
  created_at                DateTime? @default(now()) @db.Timestamptz(6)
  updated_at                DateTime? @db.Timestamptz(6)
  remember_token            String?   @default("_utf8mb4\\\\'NULL\\\\'") @db.VarChar(255)
  two_factor_auth           Boolean?  @default(false)
  person_id                 BigInt?
  name                      String?   @db.VarChar(255)
  role                      String    @db.VarChar(255)
  is_google_auth            BigInt?
  avatar                    String?   @db.VarChar(255)
  cover                     String?   @db.VarChar(255)
  person_type               String?   @db.VarChar(255)

  @@index([person_id], map: "idx_21416_fk_accounts_faculty")
}

model activity_log {
  id           BigInt    @id(map: "idx_21468_primary") @default(autoincrement())
  log_name     String?   @db.VarChar(255)
  description  String
  subject_type String?   @db.VarChar(255)
  event        String?   @db.VarChar(255)
  subject_id   Decimal?  @db.Decimal
  causer_type  String?   @db.VarChar(255)
  causer_id    Decimal?  @db.Decimal
  properties   String?
  batch_uuid   BigInt?
  created_at   DateTime? @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)

  @@index([log_name], map: "idx_21468_activity_log_log_name_index")
  @@index([causer_type, causer_id], map: "idx_21468_causer")
  @@index([subject_type, subject_id], map: "idx_21468_subject")
}

model additional_fees {
  id                      BigInt             @id @default(autoincrement())
  enrollment_id           BigInt
  fee_name                String             @db.VarChar(255)
  description             String?
  amount                  Decimal            @db.Decimal(10, 2)
  is_required             Boolean            @default(false)
  sort_order              Int                @default(0)
  created_at              DateTime?          @db.Timestamp(0)
  updated_at              DateTime?          @db.Timestamp(0)
  is_separate_transaction Boolean            @default(false)
  transaction_number      String?            @db.VarChar(255)
  student_enrollment      student_enrollment @relation(fields: [enrollment_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "additional_fees_enrollment_id_foreign")

  @@index([enrollment_id, sort_order], map: "additional_fees_enrollment_id_sort_order_index")
}

model admin_transactions {
  id             BigInt       @id(map: "idx_21479_primary") @default(autoincrement())
  admin_id       BigInt
  transaction_id BigInt
  status         String       @db.VarChar(255)
  created_at     DateTime?    @db.Timestamptz(6)
  updated_at     DateTime?    @db.Timestamptz(6)
  users          users        @relation(fields: [admin_id], references: [id], onUpdate: Restrict, map: "admin_transactions_admin_id_foreign")
  transactions   transactions @relation(fields: [transaction_id], references: [id], onUpdate: Restrict, map: "admin_transactions_transaction_id_foreign")

  @@index([admin_id], map: "idx_21479_admin_transactions_admin_id_foreign")
  @@index([transaction_id], map: "idx_21479_admin_transactions_transaction_id_foreign")
}

model announcements {
  id          BigInt    @id(map: "idx_21484_primary") @default(autoincrement())
  title       String    @db.VarChar(255)
  content     String
  slug        String    @db.VarChar(255)
  status      String    @db.VarChar(255)
  user_id     BigInt
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  attachments Json?     @db.Json
  class_id    Int?
  users       users     @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "announcements_user_id_foreign")

  @@index([user_id], map: "idx_21484_announcements_user_id_foreign")
  @@index([class_id], map: "idx_announcements_class_id")
}

model attendances {
  id                  BigInt    @id(map: "idx_21491_primary") @default(autoincrement())
  class_enrollment_id BigInt
  student_id          BigInt
  date                DateTime  @db.Date
  status              String    @default("present") @db.VarChar(255)
  created_at          DateTime? @db.Timestamptz(6)
  updated_at          DateTime? @db.Timestamptz(6)
  class_id            Int
  remarks             String?
  marked_at           DateTime? @db.Timestamp(0)
  marked_by           String?   @db.Uuid
  ip_address          String?   @db.VarChar(255)
  location_data       Json?     @db.Json
  deleted_at          DateTime? @db.Timestamp(0)
  classes             classes   @relation(fields: [class_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "attendances_class_id_foreign")
  faculty             faculty?  @relation(fields: [marked_by], references: [id], onUpdate: NoAction, map: "attendances_marked_by_foreign")

  @@index([class_id, date], map: "idx_attendances_class_date")
  @@index([date, status], map: "idx_attendances_date_status")
  @@index([student_id, date], map: "idx_attendances_student_date")
}

model authentication_log {
  id                   BigInt    @id(map: "idx_21497_primary") @default(autoincrement())
  authenticatable_type String    @db.VarChar(255)
  authenticatable_id   Decimal   @db.Decimal
  ip_address           String?   @db.VarChar(45)
  user_agent           String?
  login_at             DateTime? @db.Timestamptz(6)
  login_successful     Boolean   @default(false)
  logout_at            DateTime? @db.Timestamptz(6)
  cleared_by_user      Boolean   @default(false)
  location             String?
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)

  @@index([authenticatable_type, authenticatable_id], map: "idx_21497_authentication_log_authenticatable_type_authenticatab")
}

model authenticator_apps {
  id           BigInt    @id(map: "idx_21507_primary") @default(autoincrement())
  user_id      BigInt
  name         String?   @db.VarChar(255)
  secret       String?
  last_used_at DateTime? @db.Timestamptz(6)
  created_at   DateTime? @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)
  users        users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "authenticator_apps_user_id_foreign")

  @@index([user_id], map: "idx_21507_authenticator_apps_user_id_foreign")
}

model authors {
  id          BigInt    @id @default(autoincrement())
  name        String    @db.VarChar(255)
  biography   String?
  birth_date  DateTime? @db.Date
  nationality String?   @db.VarChar(255)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  deleted_at  DateTime? @db.Timestamp(0)

  @@index([name], map: "authors_name_index")
}

model backup_destination_statuses {
  id          BigInt    @id(map: "idx_21515_primary") @default(autoincrement())
  name        String    @db.VarChar(255)
  disk        String    @db.VarChar(255)
  reachable   String    @db.VarChar(255)
  healthy     String    @db.VarChar(255)
  amount      BigInt
  newest      String?   @db.VarChar(255)
  usedstorage String    @db.VarChar(255)
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

model bookings {
  id                                BigInt            @id @default(autoincrement())
  resource_booking_id               BigInt
  booked_by                         BigInt
  start_datetime                    DateTime          @db.Timestamp(0)
  end_datetime                      DateTime          @db.Timestamp(0)
  purpose                           String            @db.VarChar(255)
  notes                             String?
  expected_attendees                Int?
  status                            String            @default("pending") @db.VarChar(255)
  total_cost                        Decimal?          @db.Decimal(10, 2)
  approved_by                       BigInt?
  approved_at                       DateTime?         @db.Timestamp(0)
  approval_notes                    String?
  cancelled_at                      DateTime?         @db.Timestamp(0)
  cancellation_reason               String?
  additional_requirements           Json?             @db.Json
  created_at                        DateTime?         @db.Timestamp(0)
  updated_at                        DateTime?         @db.Timestamp(0)
  users_bookings_approved_byTousers users?            @relation("bookings_approved_byTousers", fields: [approved_by], references: [id], onUpdate: NoAction, map: "bookings_approved_by_foreign")
  users_bookings_booked_byTousers   users             @relation("bookings_booked_byTousers", fields: [booked_by], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "bookings_booked_by_foreign")
  resource_bookings                 resource_bookings @relation(fields: [resource_booking_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "bookings_resource_booking_id_foreign")

  @@index([booked_by, status], map: "bookings_booked_by_status_index")
  @@index([resource_booking_id, start_datetime, end_datetime], map: "bookings_resource_booking_id_start_datetime_end_datetime_index")
  @@index([status, start_datetime], map: "bookings_status_start_datetime_index")
}

model breezy_sessions {
  id                        BigInt    @id(map: "idx_21523_primary") @default(autoincrement())
  authenticatable_type      String    @db.VarChar(255)
  authenticatable_id        BigInt
  panel_id                  String?   @db.VarChar(255)
  guard                     String?   @db.VarChar(255)
  ip_address                String?   @db.VarChar(255)
  user_agent                String?
  expires_at                DateTime? @db.Timestamptz(6)
  two_factor_secret         String?
  two_factor_recovery_codes String?
  two_factor_confirmed_at   DateTime? @db.Timestamptz(6)
  created_at                DateTime? @db.Timestamptz(6)
  updated_at                DateTime? @db.Timestamptz(6)

  @@index([authenticatable_type, authenticatable_id], map: "idx_21523_breezy_sessions_authenticatable_type_authenticatable_")
}

model bulk_action_records {
  id             BigInt       @id(map: "idx_21542_primary") @default(autoincrement())
  bulk_action_id BigInt
  record_id      BigInt
  record_type    String       @db.VarChar(255)
  status         String       @default("queued") @db.VarChar(255)
  message        String?
  started_at     DateTime?    @db.Timestamptz(6)
  failed_at      DateTime?    @db.Timestamptz(6)
  finished_at    DateTime?    @db.Timestamptz(6)
  created_at     DateTime?    @db.Timestamptz(6)
  updated_at     DateTime?    @db.Timestamptz(6)
  bulk_actions   bulk_actions @relation(fields: [bulk_action_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "bulk_action_records_bulk_action_id_foreign")

  @@index([bulk_action_id], map: "idx_21542_bulk_action_records_bulk_action_id_foreign")
  @@index([record_id, record_type], map: "idx_21542_bulk_action_records_record_id_record_type_index")
}

model bulk_actions {
  id                  BigInt                @id(map: "idx_21533_primary") @default(autoincrement())
  name                String?               @db.VarChar(255)
  type                String                @db.VarChar(255)
  identifier          String                @db.VarChar(255)
  status              String                @default("queued") @db.VarChar(255)
  job                 String                @db.VarChar(255)
  user_id             BigInt?
  total_records       BigInt?
  data                String?
  message             String?
  dismissed_at        DateTime?             @db.Timestamptz(6)
  started_at          DateTime?             @db.Timestamptz(6)
  failed_at           DateTime?             @db.Timestamptz(6)
  finished_at         DateTime?             @db.Timestamptz(6)
  created_at          DateTime?             @db.Timestamptz(6)
  updated_at          DateTime?             @db.Timestamptz(6)
  bulk_action_records bulk_action_records[]
  users               users?                @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "bulk_actions_user_id_foreign")

  @@index([type, identifier], map: "idx_21533_bulk_actions_type_identifier_index")
  @@index([user_id], map: "idx_21533_bulk_actions_user_id_foreign")
}

model cache {
  key        String  @id(map: "idx_21549_primary") @db.VarChar(255)
  value      String
  expiration BigInt
  created_at String? @db.VarChar
  updated_at String? @db.VarChar
}

model cache_locks {
  key        String   @id(map: "idx_21554_primary") @db.VarChar(255)
  owner      String   @db.VarChar(255)
  expiration BigInt
  created_at DateTime @default(now()) @db.Timestamp(0)
  updated_at DateTime @db.Timestamp(0)
}

model categories {
  id         BigInt    @id(map: "idx_21560_primary") @default(autoincrement())
  name       String    @db.VarChar(255)
  slug       String    @db.VarChar(255)
  is_visible Boolean   @default(true)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model category_ticket {
  category_id Decimal   @db.Decimal
  ticket_id   Decimal   @db.Decimal
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)

  @@ignore
}

model ch_favorites {
  id          String    @id(map: "idx_21572_primary") @db.Char(36)
  user_id     BigInt
  favorite_id BigInt
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

model ch_messages {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  from_id    BigInt
  to_id      BigInt
  body       String?   @db.VarChar(5000)
  attachment String?   @db.VarChar(255)
  seen       Boolean   @default(false)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model class_attendance_settings {
  id                           BigInt    @id @default(autoincrement())
  class_id                     Int       @unique(map: "class_attendance_settings_class_id_unique")
  faculty_id                   String    @db.Uuid
  is_enabled                   Boolean   @default(false)
  start_date                   DateTime? @db.Date
  end_date                     DateTime? @db.Date
  attendance_method            String    @default("manual") @db.VarChar(255)
  attendance_policy            String    @default("present_by_default") @db.VarChar(255)
  grace_period_minutes         Int       @default(15)
  auto_mark_absent_minutes     Int?
  allow_late_checkin           Boolean   @default(true)
  checkin_start_time           DateTime? @db.Time(0)
  checkin_end_time             DateTime? @db.Time(0)
  qr_code_token                String?   @db.VarChar(255)
  qr_code_expires_at           DateTime? @db.Timestamp(0)
  qr_code_auto_refresh         Boolean   @default(true)
  qr_code_refresh_minutes      Int       @default(30)
  attendance_code              String?   @db.VarChar(255)
  attendance_code_expires_at   DateTime? @db.Timestamp(0)
  attendance_code_auto_refresh Boolean   @default(false)
  require_confirmation         Boolean   @default(false)
  show_class_list              Boolean   @default(true)
  notify_absent_students       Boolean   @default(false)
  notify_late_students         Boolean   @default(false)
  send_daily_summary           Boolean   @default(false)
  additional_settings          Json?     @db.Json
  notes                        String?
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  classes                      classes   @relation(fields: [class_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "class_attendance_settings_class_id_foreign")
  faculty                      faculty   @relation(fields: [faculty_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "class_attendance_settings_faculty_id_foreign")

  @@index([faculty_id], map: "class_attendance_settings_faculty_id_index")
  @@index([is_enabled], map: "class_attendance_settings_is_enabled_index")
}

model class_enrollments {
  id                   BigInt    @id(map: "idx_21599_primary") @default(autoincrement())
  class_id             Int
  student_id           Decimal   @db.Decimal
  completion_date      DateTime? @db.Date
  status               Boolean   @default(true)
  remarks              String?
  prelim_grade         Decimal?  @db.Decimal(8, 2)
  midterm_grade        Decimal?  @db.Decimal(8, 2)
  finals_grade         Decimal?  @db.Decimal(8, 2)
  total_average        Decimal?  @db.Decimal(8, 2)
  created_at           DateTime? @db.Timestamptz(6)
  updated_at           DateTime? @db.Timestamptz(6)
  deleted_at           DateTime? @db.Timestamptz(6)
  is_grades_finalized  Boolean   @default(false)
  is_grades_verified   Boolean   @default(false)
  verified_by          BigInt?
  verified_at          DateTime? @db.Timestamp(0)
  verification_notes   String?
  is_finals_submitted  Boolean   @default(false)
  is_midterm_submitted Boolean   @default(false)
  is_prelim_submitted  Boolean   @default(false)
  classes              classes   @relation(fields: [class_id], references: [id], onUpdate: Restrict, map: "class_enrollments_class_id_foreign")
  users                users?    @relation(fields: [verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "class_enrollments_verified_by_foreign")

  @@index([class_id], map: "idx_21599_class_enrollments_class_id_foreign")
  @@index([student_id], map: "idx_21599_class_enrollments_student_id_foreign")
  @@index([class_id, status], map: "idx_class_enrollments_class_status")
  @@index([student_id, class_id], map: "idx_class_enrollments_student_class")
  @@index([student_id, status], map: "idx_class_enrollments_student_status")
}

model class_posts {
  id         BigInt    @id(map: "idx_21611_primary") @default(autoincrement())
  user_id    BigInt
  class_id   Int
  content    String
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
  classes    classes   @relation(fields: [class_id], references: [id], onUpdate: Restrict, map: "class_posts_class_id_foreign")
  users      users     @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "class_posts_user_id_foreign")

  @@index([class_id], map: "idx_21611_class_posts_class_id_foreign")
  @@index([user_id], map: "idx_21611_class_posts_user_id_foreign")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model classes {
  id                        Int                        @id(map: "idx_21584_primary") @default(autoincrement())
  subject_code              String?                    @db.VarChar(255)
  faculty_id                String?                    @db.Uuid
  academic_year             String?                    @db.VarChar(255)
  semester                  String?                    @db.VarChar(255)
  schedule_id               Int?
  school_year               String?                    @db.VarChar(255)
  course_codes              String?                    @db.VarChar(255)
  section                   String?                    @db.VarChar(255)
  room_id                   Int?                       @db.SmallInt
  classification            String?                    @db.VarChar(255)
  maximum_slots             Int?                       @default(50) @db.SmallInt
  created_at                DateTime?                  @db.Timestamp(0)
  updated_at                DateTime?                  @db.Timestamp(0)
  shs_track_id              BigInt?
  shs_strand_id             BigInt?
  grade_level               String?                    @db.VarChar(255)
  subject_id                BigInt?
  subject_ids               Json?                      @db.Json
  attendances               attendances[]
  class_attendance_settings class_attendance_settings?
  class_enrollments         class_enrollments[]
  class_posts               class_posts[]
  shs_strands               shs_strands?               @relation(fields: [shs_strand_id], references: [id], onUpdate: NoAction, map: "classes_shs_strand_id_foreign")
  shs_tracks                shs_tracks?                @relation(fields: [shs_track_id], references: [id], onUpdate: NoAction, map: "classes_shs_track_id_foreign")
  subject                   subject?                   @relation(fields: [subject_id], references: [id], onUpdate: NoAction, map: "classes_subject_id_foreign")
  faculty_deadlines         faculty_deadlines[]
  missing_student_requests  missing_student_requests[]

  @@index([academic_year, semester], map: "idx_21584_classes_academic_year_semester_index")
  @@index([course_codes], map: "idx_21584_classes_course_codes_index")
  @@index([faculty_id], map: "idx_21584_classes_faculty_id_index")
  @@index([school_year], map: "idx_21584_classes_school_year_index")
  @@index([subject_code], map: "idx_21584_classes_subject_code_index")
  @@index([faculty_id], map: "idx_21584_fk_classes_facultyb")
  @@index([subject_code], map: "idx_21584_fk_classes_subject1")
  @@index([academic_year, semester], map: "idx_21584_idx_academic_year_semester")
  @@index([schedule_id], map: "idx_21584_idx_classes_schedule_id")
  @@index([classification], map: "idx_classes_classification")
  @@index([faculty_id], map: "idx_classes_faculty_id")
  @@index([subject_id], map: "idx_classes_subject_id")
  @@index([subject_code, school_year, semester], map: "idx_classes_subject_year_semester")
}

model clients {
  id         BigInt    @id(map: "idx_21618_primary") @default(autoincrement())
  name       String    @db.VarChar(255)
  iban       String?   @db.VarChar(255)
  kvk_number String?   @db.VarChar(255)
  vat_number String?   @db.VarChar(255)
  address    String?   @db.VarChar(255)
  city       String?   @db.VarChar(255)
  zipcode    String?   @db.VarChar(255)
  country    String?   @db.VarChar(255)
  phone      String?   @db.VarChar(255)
  email      String?   @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model connected_accounts {
  id            BigInt    @id(map: "idx_21634_primary") @default(autoincrement())
  user_id       Decimal   @db.Decimal
  provider      String    @db.VarChar(255)
  provider_id   String    @db.VarChar(255)
  name          String?   @db.VarChar(255)
  nickname      String?   @db.VarChar(255)
  email         String?   @db.VarChar(255)
  telephone     String?   @db.VarChar(255)
  avatar_path   String?
  token         String    @db.VarChar(1000)
  secret        String?   @db.VarChar(255)
  refresh_token String?   @db.VarChar(1000)
  expires_at    DateTime? @db.Timestamptz(6)
  created_at    DateTime? @db.Timestamptz(6)
  updated_at    DateTime? @db.Timestamptz(6)

  @@index([provider, provider_id], map: "idx_21634_connected_accounts_provider_provider_id_index")
  @@index([user_id, id], map: "idx_21634_connected_accounts_user_id_id_index")
}

model contacts {
  id         BigInt    @id(map: "idx_21647_primary") @default(autoincrement())
  type       String?   @default("contact") @db.VarChar(255)
  status     String?   @default("pending") @db.VarChar(255)
  name       String    @db.VarChar(255)
  email      String?   @db.VarChar(255)
  phone      String?   @db.VarChar(255)
  subject    String    @db.VarChar(255)
  message    String
  active     Boolean?  @default(true)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@index([name], map: "idx_21647_contacts_name_index")
}

model contacts_metas {
  id         BigInt    @id(map: "idx_21659_primary") @default(autoincrement())
  model_id   Decimal?  @db.Decimal
  model_type String?   @db.VarChar(255)
  contact_id Decimal   @db.Decimal
  key        String    @db.VarChar(255)
  value      String?
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@index([contact_id], map: "idx_21659_contacts_metas_contact_id_foreign")
  @@index([key], map: "idx_21659_contacts_metas_key_index")
}

model courses {
  id              BigInt    @id(map: "idx_21667_primary") @default(autoincrement())
  code            String    @db.VarChar(255)
  title           String    @db.VarChar(255)
  description     String
  department      String    @db.VarChar(255)
  lec_per_unit    BigInt?
  lab_per_unit    BigInt?
  remarks         String?
  curriculum_year String?   @db.VarChar(255)
  miscelaneous    Int?
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  units           Int       @default(0)
  year_level      Int       @default(1)
  semester        Int       @default(1)
  school_year     String?   @db.VarChar(255)
  miscellaneous   String?   @db.VarChar(255)
  is_active       Boolean   @default(true)

  @@index([code], map: "idx_21667_idx_code")
}

model credited_enrolled_subjects {
  id         BigInt    @id(map: "idx_21675_primary") @default(autoincrement())
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model departments {
  id          BigInt    @id @default(autoincrement())
  school_id   BigInt
  name        String    @db.VarChar(255)
  code        String    @db.VarChar(10)
  description String?
  head_name   String?   @db.VarChar(255)
  head_email  String?   @db.VarChar(255)
  location    String?   @db.VarChar(255)
  phone       String?   @db.VarChar(255)
  email       String?   @db.VarChar(255)
  is_active   Boolean   @default(true)
  metadata    Json?     @db.Json
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  schools     schools   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "departments_school_id_foreign")
  users       users[]

  @@unique([school_id, code], map: "departments_school_id_code_unique")
  @@unique([school_id, name], map: "departments_school_id_name_unique")
  @@index([code], map: "departments_code_index")
  @@index([school_id, is_active], map: "departments_school_id_is_active_index")
}

model document_locations {
  id                   BigInt    @id(map: "idx_21680_primary") @default(autoincrement())
  birth_certificate    String?   @db.VarChar(255)
  form_138             String?   @db.VarChar(255)
  form_137             String?   @db.VarChar(255)
  good_moral_cert      String?   @db.VarChar(255)
  transfer_credentials String?   @db.VarChar(255)
  transcript_records   String?   @db.VarChar(255)
  picture_1x1          String?   @db.VarChar(255)
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
}

model enrollment_signatures {
  id                  BigInt    @id(map: "idx_21694_primary") @default(autoincrement())
  depthead_signature  String?
  registrar_signature String?
  cashier_signature   String?
  enrollment_id       BigInt?
  enrollment_type     String?
  created_at          DateTime? @db.Timestamptz(6)
  updated_at          DateTime? @db.Timestamptz(6)
}

model event_reminders {
  id             BigInt    @id @default(autoincrement())
  event_id       BigInt
  user_id        BigInt
  reminder_type  String    @db.VarChar(255)
  minutes_before Int
  status         String    @default("pending") @db.VarChar(255)
  scheduled_at   DateTime  @db.Timestamp(0)
  sent_at        DateTime? @db.Timestamp(0)
  message        String?
  delivery_data  Json?     @db.Json
  failure_reason String?
  retry_count    Int       @default(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
  events         events    @relation(fields: [event_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "event_reminders_event_id_foreign")
  users          users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "event_reminders_user_id_foreign")

  @@index([event_id, scheduled_at], map: "event_reminders_event_id_scheduled_at_index")
  @@index([scheduled_at, status], map: "event_reminders_scheduled_at_status_index")
  @@index([user_id, status], map: "event_reminders_user_id_status_index")
}

model event_rsvps {
  id                   BigInt    @id @default(autoincrement())
  event_id             BigInt
  user_id              BigInt
  response             String    @default("pending") @db.VarChar(255)
  guest_count          Int       @default(0)
  dietary_requirements String?
  special_requests     String?
  notes                String?
  responded_at         DateTime? @db.Timestamp(0)
  checked_in           Boolean   @default(false)
  checked_in_at        DateTime? @db.Timestamp(0)
  custom_responses     Json?     @db.Json
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  events               events    @relation(fields: [event_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "event_rsvps_event_id_foreign")
  users                users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "event_rsvps_user_id_foreign")

  @@unique([event_id, user_id], map: "event_rsvps_event_id_user_id_unique")
  @@index([event_id, response], map: "event_rsvps_event_id_response_index")
  @@index([user_id, response], map: "event_rsvps_user_id_response_index")
}

model events {
  id                  BigInt            @id @default(autoincrement())
  title               String            @db.VarChar(255)
  description         String?
  type                String            @default("event") @db.VarChar(255)
  category            String?           @db.VarChar(255)
  location            String?           @db.VarChar(255)
  start_datetime      DateTime          @db.Timestamp(0)
  end_datetime        DateTime          @db.Timestamp(0)
  is_all_day          Boolean           @default(false)
  recurrence_type     String?           @db.VarChar(255)
  recurrence_data     Json?             @db.Json
  recurrence_end_date DateTime?         @db.Timestamp(0)
  max_attendees       Int?
  requires_rsvp       Boolean           @default(false)
  allow_guests        Boolean           @default(true)
  status              String            @default("active") @db.VarChar(255)
  visibility          String            @default("public") @db.VarChar(255)
  custom_fields       Json?             @db.Json
  notes               String?
  created_by          BigInt
  created_at          DateTime?         @db.Timestamp(0)
  updated_at          DateTime?         @db.Timestamp(0)
  event_reminders     event_reminders[]
  event_rsvps         event_rsvps[]
  users               users             @relation(fields: [created_by], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "events_created_by_foreign")

  @@index([created_by, start_datetime], map: "events_created_by_start_datetime_index")
  @@index([start_datetime, end_datetime], map: "events_start_datetime_end_datetime_index")
  @@index([type, status], map: "events_type_status_index")
}

model exception_log_groups {
  id         BigInt    @id(map: "idx_21709_primary") @default(autoincrement())
  message    String
  type       String    @db.VarChar(255)
  file       String    @db.VarChar(255)
  line       BigInt
  first_seen DateTime  @db.Timestamptz(6)
  last_seen  DateTime  @db.Timestamptz(6)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
  site_id    BigInt

  @@index([site_id], map: "idx_21709_exception_log_groups_site_id_foreign")
}

model exception_logs {
  id                     BigInt    @id(map: "idx_21701_primary") @default(autoincrement())
  message                String
  type                   String    @db.VarChar(255)
  file                   String    @db.VarChar(255)
  status                 String    @default("unresolved") @db.VarChar(255)
  line                   BigInt
  trace                  String
  request                String?
  thrown_at              DateTime  @db.Timestamptz(6)
  exception_log_group_id BigInt
  created_at             DateTime? @db.Timestamptz(6)
  updated_at             DateTime? @db.Timestamptz(6)

  @@index([exception_log_group_id], map: "idx_21701_exception_logs_exception_log_group_id_foreign")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model export_jobs {
  id            BigInt    @id @default(autoincrement())
  job_id        String    @unique(map: "export_jobs_job_id_unique") @db.VarChar(255)
  user_id       BigInt
  export_type   String    @default("student_data") @db.VarChar(255)
  filters       Json      @db.Json
  format        String    @db.VarChar(255)
  status        String    @default("pending") @db.VarChar(255)
  file_content  String?
  file_name     String?   @db.VarChar(255)
  error_message String?
  started_at    DateTime? @db.Timestamp(0)
  completed_at  DateTime? @db.Timestamp(0)
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  users         users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "export_jobs_user_id_foreign")

  @@index([job_id], map: "export_jobs_job_id_index")
  @@index([user_id, status], map: "export_jobs_user_id_status_index")
}

model exports {
  id              BigInt    @id(map: "idx_21716_primary") @default(autoincrement())
  completed_at    DateTime? @db.Timestamptz(6)
  file_disk       String    @db.VarChar(255)
  file_name       String?   @db.VarChar(255)
  exporter        String    @db.VarChar(255)
  processed_rows  BigInt    @default(0)
  total_rows      BigInt
  successful_rows BigInt    @default(0)
  user_id         BigInt?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
  users           users?    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "exports_user_id_foreign")

  @@index([user_id], map: "idx_21716_exports_user_id_foreign")
}

model faculty {
  first_name                String                      @db.VarChar(255)
  last_name                 String                      @db.VarChar(255)
  middle_name               String?                     @db.VarChar(255)
  email                     String                      @unique(map: "faculty_email_unique") @db.VarChar(255)
  phone_number              String?                     @db.VarChar(255)
  department                String?                     @db.VarChar(255)
  office_hours              String?                     @db.VarChar(255)
  birth_date                DateTime?                   @db.Date
  address_line1             String?                     @db.VarChar(255)
  biography                 String?
  education                 String?
  courses_taught            String?
  photo_url                 String?                     @db.VarChar(255)
  status                    faculty_status?
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  gender                    faculty_gender?
  age                       BigInt?
  password                  String?                     @db.VarChar(255)
  remember_token            String?                     @db.VarChar(100)
  email_verified_at         DateTime?                   @db.Timestamp(0)
  id                        String                      @id @db.Uuid
  faculty_code              String?                     @unique(map: "faculty_faculty_code_unique") @db.VarChar(20)
  faculty_id_number         String?                     @unique(map: "faculty_faculty_id_number_unique") @db.VarChar(20)
  full_name                 String?                     @db.VarChar(255)
  attendances               attendances[]
  class_attendance_settings class_attendance_settings[]
  faculty_deadlines         faculty_deadlines[]
  missing_student_requests  missing_student_requests[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model faculty_deadlines {
  id          BigInt    @id @default(autoincrement())
  title       String    @db.VarChar(255)
  description String?
  due_date    DateTime  @db.Timestamp(0)
  priority    String    @default("medium") @db.VarChar(255)
  type        String?   @db.VarChar(255)
  faculty_id  String    @db.Uuid
  class_id    Int?
  is_active   Boolean   @default(true)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  classes     classes?  @relation(fields: [class_id], references: [id], onUpdate: NoAction, map: "faculty_deadlines_class_id_foreign")
  faculty     faculty   @relation(fields: [faculty_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "faculty_deadlines_faculty_id_foreign")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model faculty_notifications {
  id          BigInt    @id @default(autoincrement())
  user_id     BigInt
  title       String    @db.VarChar(255)
  message     String
  type        String    @default("info") @db.VarChar(255)
  data        Json?     @db.Json
  action_url  String?   @db.VarChar(255)
  action_text String?   @db.VarChar(255)
  is_read     Boolean   @default(false)
  read_at     DateTime? @db.Timestamp(0)
  priority    String    @default("normal") @db.VarChar(255)
  expires_at  DateTime? @db.Timestamp(0)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  accounts    accounts  @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "faculty_notifications_user_id_foreign")

  @@index([priority], map: "faculty_notifications_priority_index")
  @@index([type], map: "faculty_notifications_type_index")
  @@index([user_id, created_at], map: "faculty_notifications_user_id_created_at_index")
  @@index([user_id, is_read], map: "faculty_notifications_user_id_is_read_index")
}

model failed_import_rows {
  id               BigInt    @id(map: "idx_21739_primary") @default(autoincrement())
  data             String
  import_id        Decimal   @db.Decimal
  validation_error String?
  user_id          BigInt?
  created_at       DateTime? @db.Timestamptz(6)
  updated_at       DateTime? @db.Timestamptz(6)
  users            users?    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "failed_import_rows_user_id_foreign")

  @@index([import_id], map: "idx_21739_failed_import_rows_import_id_foreign")
  @@index([user_id], map: "idx_21739_failed_import_rows_user_id_foreign")
}

model failed_jobs {
  id         BigInt   @id(map: "idx_21746_primary") @default(autoincrement())
  uuid       String   @unique(map: "idx_21746_failed_jobs_uuid_unique") @db.VarChar(255)
  connection String
  queue      String
  payload    String
  exception  String
  failed_at  DateTime @default(now()) @db.Timestamptz(6)
}

model fblog_categories {
  id                        BigInt                      @id(map: "idx_21754_primary") @default(autoincrement())
  name                      String                      @unique(map: "idx_21754_fblog_categories_name_unique") @db.VarChar(255)
  slug                      String                      @unique(map: "idx_21754_fblog_categories_slug_unique") @db.VarChar(255)
  created_at                DateTime?                   @db.Timestamptz(6)
  updated_at                DateTime?                   @db.Timestamptz(6)
  fblog_category_fblog_post fblog_category_fblog_post[]
}

model fblog_category_fblog_post {
  id               BigInt           @id(map: "idx_21761_primary") @default(autoincrement())
  post_id          BigInt
  category_id      BigInt
  created_at       DateTime?        @db.Timestamptz(6)
  updated_at       DateTime?        @db.Timestamptz(6)
  fblog_categories fblog_categories @relation(fields: [category_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_category_fblog_post_category_id_foreign")
  fblog_posts      fblog_posts      @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_category_fblog_post_post_id_foreign")

  @@index([category_id], map: "idx_21761_fblog_category_fblog_post_category_id_foreign")
  @@index([post_id], map: "idx_21761_fblog_category_fblog_post_post_id_foreign")
}

model fblog_comments {
  id          BigInt      @id(map: "idx_21766_primary") @default(autoincrement())
  user_id     BigInt
  post_id     BigInt
  comment     String
  approved    Boolean     @default(false)
  approved_at DateTime?   @db.Timestamptz(6)
  created_at  DateTime?   @db.Timestamptz(6)
  updated_at  DateTime?   @db.Timestamptz(6)
  fblog_posts fblog_posts @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_comments_post_id_foreign")

  @@index([post_id], map: "idx_21766_fblog_comments_post_id_foreign")
}

model fblog_news_letters {
  id         BigInt    @id(map: "idx_21774_primary") @default(autoincrement())
  email      String    @unique(map: "idx_21774_fblog_news_letters_email_unique") @db.VarChar(255)
  subscribed Boolean   @default(true)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model fblog_post_fblog_tag {
  id          BigInt      @id(map: "idx_21789_primary") @default(autoincrement())
  post_id     BigInt
  tag_id      BigInt
  created_at  DateTime?   @db.Timestamptz(6)
  updated_at  DateTime?   @db.Timestamptz(6)
  fblog_posts fblog_posts @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_post_fblog_tag_post_id_foreign")
  fblog_tags  fblog_tags  @relation(fields: [tag_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_post_fblog_tag_tag_id_foreign")

  @@index([post_id], map: "idx_21789_fblog_post_fblog_tag_post_id_foreign")
  @@index([tag_id], map: "idx_21789_fblog_post_fblog_tag_tag_id_foreign")
}

model fblog_posts {
  id                        BigInt                      @id(map: "idx_21780_primary") @default(autoincrement())
  title                     String                      @db.VarChar(255)
  slug                      String                      @db.VarChar(255)
  sub_title                 String?                     @db.VarChar(255)
  body                      String
  status                    fblog_posts_status          @default(pending)
  published_at              DateTime?                   @db.Timestamptz(6)
  scheduled_for             DateTime?                   @db.Timestamptz(6)
  cover_photo_path          String                      @db.VarChar(255)
  photo_alt_text            String                      @db.VarChar(255)
  user_id                   BigInt
  created_at                DateTime?                   @db.Timestamptz(6)
  updated_at                DateTime?                   @db.Timestamptz(6)
  fblog_category_fblog_post fblog_category_fblog_post[]
  fblog_comments            fblog_comments[]
  fblog_post_fblog_tag      fblog_post_fblog_tag[]
  users                     users                       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_posts_user_id_foreign")
  fblog_seo_details         fblog_seo_details[]

  @@index([user_id], map: "idx_21780_fblog_posts_user_id_foreign")
}

model fblog_seo_details {
  id          BigInt      @id(map: "idx_21794_primary") @default(autoincrement())
  post_id     BigInt
  title       String      @db.VarChar(255)
  keywords    String?
  description String
  created_at  DateTime?   @db.Timestamptz(6)
  updated_at  DateTime?   @db.Timestamptz(6)
  fblog_posts fblog_posts @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fblog_seo_details_post_id_foreign")

  @@index([post_id], map: "idx_21794_fblog_seo_details_post_id_foreign")
}

model fblog_settings {
  id                   BigInt    @id(map: "idx_21801_primary") @default(autoincrement())
  title                String?   @db.VarChar(255)
  description          String?
  logo                 String?   @db.VarChar(255)
  favicon              String?   @db.VarChar(255)
  organization_name    String?   @db.VarChar(255)
  google_console_code  String?
  google_analytic_code String?
  google_adsense_code  String?
  quick_links          String?
  created_at           DateTime? @db.Timestamptz(6)
  updated_at           DateTime? @db.Timestamptz(6)
}

model fblog_share_snippets {
  id          BigInt    @id(map: "idx_21812_primary") @default(autoincrement())
  script_code String
  html_code   String
  active      Boolean   @default(true)
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

model fblog_tags {
  id                   BigInt                 @id(map: "idx_21820_primary") @default(autoincrement())
  name                 String                 @unique(map: "idx_21820_fblog_tags_name_unique") @db.VarChar(255)
  slug                 String                 @unique(map: "idx_21820_fblog_tags_slug_unique") @db.VarChar(255)
  created_at           DateTime?              @db.Timestamptz(6)
  updated_at           DateTime?              @db.Timestamptz(6)
  fblog_post_fblog_tag fblog_post_fblog_tag[]
}

model feature_segments {
  id         BigInt    @id(map: "idx_21834_primary") @default(autoincrement())
  feature    String    @db.VarChar(255)
  scope      String    @db.VarChar(255)
  values     String
  active     Boolean
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@unique([feature, scope, active], map: "idx_21834_feature_segments_feature_scope_active_unique")
}

model features {
  id         BigInt    @id(map: "idx_21827_primary") @default(autoincrement())
  name       String    @db.VarChar(255)
  scope      String    @db.VarChar(255)
  value      String
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@unique([name, scope], map: "idx_21827_features_name_scope_unique")
}

model feedbacks {
  id         BigInt    @id(map: "idx_21841_primary") @default(autoincrement())
  type       String    @db.VarChar(255)
  message    String
  user_info  String?
  reviewed   Boolean   @default(false)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@index([type], map: "idx_21841_feedbacks_type_index")
}

model filachat_agents {
  id             BigInt    @id(map: "idx_21849_primary") @default(autoincrement())
  agentable_id   Decimal   @db.Decimal
  agentable_type String    @db.VarChar(255)
  created_at     DateTime? @db.Timestamptz(6)
  updated_at     DateTime? @db.Timestamptz(6)

  @@unique([agentable_id, agentable_type], map: "idx_21849_filachat_agents_agentable_id_agentable_type_unique")
}

model filachat_conversations {
  id                BigInt    @id(map: "idx_21856_primary") @default(autoincrement())
  senderable_id     Decimal   @db.Decimal
  senderable_type   String    @db.VarChar(255)
  receiverable_id   Decimal   @db.Decimal
  receiverable_type String    @db.VarChar(255)
  created_at        DateTime? @db.Timestamptz(6)
  updated_at        DateTime? @db.Timestamptz(6)
}

model filachat_messages {
  id                             BigInt    @id(map: "idx_21863_primary") @default(autoincrement())
  filachat_conversation_id       Decimal   @db.Decimal
  message                        String?
  attachments                    String?
  original_attachment_file_names String?
  reactions                      String?
  is_starred                     Boolean   @default(false)
  metadata                       String?
  reply_to_message_id            Decimal?  @db.Decimal
  senderable_id                  Decimal   @db.Decimal
  senderable_type                String    @db.VarChar(255)
  receiverable_id                Decimal   @db.Decimal
  receiverable_type              String    @db.VarChar(255)
  last_read_at                   DateTime? @db.Timestamptz(6)
  edited_at                      DateTime? @db.Timestamptz(6)
  sender_deleted_at              DateTime? @db.Timestamptz(6)
  receiver_deleted_at            DateTime? @db.Timestamptz(6)
  deleted_at                     DateTime? @db.Timestamptz(6)
  created_at                     DateTime? @db.Timestamptz(6)
  updated_at                     DateTime? @db.Timestamptz(6)

  @@index([filachat_conversation_id], map: "idx_21863_filachat_messages_filachat_conversation_id_foreign")
  @@index([receiverable_id, receiverable_type], map: "idx_21863_filachat_messages_receiverable_id_receiverable_type_i")
  @@index([reply_to_message_id], map: "idx_21863_filachat_messages_reply_to_message_id_foreign")
  @@index([senderable_id, senderable_type], map: "idx_21863_filachat_messages_senderable_id_senderable_type_index")
}

model filament_db_sync_table {
  id            BigInt    @id @default(autoincrement())
  model         String?   @db.VarChar(255)
  model_id      String?   @db.VarChar(255)
  action        String?   @db.VarChar(255)
  data          Json?     @db.Json
  status        String    @default("pending") @db.VarChar(255)
  completed_at  DateTime? @db.Timestamp(0)
  failed_at     DateTime? @db.Timestamp(0)
  failed_reason String?
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
}

model filament_email_log {
  id              BigInt    @id(map: "idx_21871_primary") @default(autoincrement())
  from            String?   @db.VarChar(255)
  to              String?   @db.VarChar(255)
  cc              String?   @db.VarChar(255)
  bcc             String?   @db.VarChar(255)
  subject         String?   @db.VarChar(255)
  text_body       String?
  html_body       String?
  raw_body        String?
  sent_debug_info String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
}

model filament_exceptions_table {
  id         BigInt    @id(map: "idx_21883_primary") @default(autoincrement())
  type       String    @db.VarChar(255)
  code       String    @db.VarChar(255)
  message    String
  file       String    @db.VarChar(255)
  line       BigInt
  trace      String
  method     String    @db.VarChar(255)
  path       String    @db.VarChar(255)
  query      String
  body       String
  cookies    String
  headers    String
  ip         String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model fileponds {
  id         BigInt    @id(map: "idx_21890_primary") @default(autoincrement())
  filename   String    @db.VarChar(255)
  filepath   String    @db.VarChar(255)
  extension  String    @db.VarChar(100)
  mimetypes  String    @db.VarChar(100)
  disk       String    @db.VarChar(100)
  created_by Decimal?  @db.Decimal
  expires_at DateTime? @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model general_settings {
  id                         BigInt    @id(map: "idx_21897_primary") @default(autoincrement())
  site_name                  String?   @db.VarChar(255)
  site_description           String?
  theme_color                String?   @db.VarChar(255)
  support_email              String?   @db.VarChar(255)
  support_phone              String?   @db.VarChar(255)
  google_analytics_id        String?   @db.VarChar(255)
  posthog_html_snippet       String?   @db.VarChar(255)
  seo_title                  String?   @db.VarChar(255)
  seo_keywords               String?   @db.VarChar(255)
  seo_metadata               String?
  email_settings             String?
  email_from_address         String?   @db.VarChar(255)
  email_from_name            String?   @db.VarChar(255)
  social_network             String?
  more_configs               String?
  created_at                 DateTime? @db.Timestamptz(6)
  updated_at                 DateTime? @db.Timestamptz(6)
  school_starting_date       String?   @db.VarChar(255)
  school_ending_date         String?   @db.VarChar(255)
  school_portal_url          String?   @db.VarChar(255)
  school_portal_enabled      Boolean?
  online_enrollment_enabled  Boolean?
  school_portal_maintenance  Boolean?
  semester                   String    @default("1") @db.VarChar(255)
  enrollment_courses         String?
  school_portal_logo         String?   @db.VarChar(255)
  school_portal_favicon      String?   @db.VarChar(255)
  school_portal_title        String?   @db.VarChar(255)
  school_portal_description  String?   @db.VarChar(255)
  enable_clearance_check     Boolean   @default(false)
  enable_signatures          Boolean   @default(false)
  enable_qr_codes            String?   @db.VarChar(255)
  enable_public_transactions String?   @db.VarChar(255)
  enable_support_page        String    @default("false") @db.VarChar(255)
  features                   String?
  curriculum_year            String?   @db.VarChar(255)
  inventory_module_enabled   Boolean   @default(false)
  library_module_enabled     Boolean   @default(false)
}

model guest_education_id {
  id                       BigInt  @id(map: "idx_21937_primary") @default(autoincrement())
  elementaryschoolname     String? @db.VarChar(255)
  elementarygraduationyear BigInt?
  seniorhighschoolname     String? @db.VarChar(255)
  seniorhighgraduationyear BigInt?
  elementaryschooladdress  String? @db.VarChar(255)
  seniorhighschooladdress  String? @db.VarChar(255)
  juniorhighschoolname     String? @db.VarChar(255)
  juniorhighgraduationyear BigInt?
  juniorhighschooladdress  String? @db.VarChar(255)
}

model guest_enrollments {
  id                     BigInt    @id(map: "idx_21950_primary") @default(autoincrement())
  selected_course        BigInt?
  academic_year          BigInt?
  selected_semester      BigInt?
  geust_education_id     BigInt?
  special_skills         String?
  guest_parents_id       BigInt?
  guest_guardian_id      BigInt?
  guest_documents_id     BigInt?
  guest_tuition_id       BigInt?
  created_at             DateTime  @default(now()) @db.Timestamptz(6)
  updated_at             DateTime? @default(now()) @db.Timestamptz(6)
  student_id             BigInt?
  guest_email            String?   @db.VarChar(255)
  status                 String?   @db.VarChar(255)
  selected_subjects      BigInt?
  deleted_at             DateTime? @db.Timestamptz(6)
  type                   String?   @db.VarChar(255)
  guest_personal_info_id BigInt?
  downpayment            BigInt?
  school_year            String?   @db.VarChar(255)
  semester               String?   @db.VarChar(255)

  @@index([geust_education_id], map: "idx_21950_fk_guest_enrollments_guest_education_id")
  @@index([guest_guardian_id], map: "idx_21950_fk_guest_enrolments_guest_guardian_contact")
  @@index([guest_documents_id], map: "idx_21950_idx_guest_enrollments_guest_documents_id")
  @@index([guest_tuition_id], map: "idx_21950_idx_guest_enrollments_guest_tuition_id")
}

model guest_guardian_contact {
  id                      BigInt  @id(map: "idx_21964_primary") @default(autoincrement())
  emergencycontactname    String? @db.VarChar(255)
  emergencycontactphone   BigInt?
  emergencycontactaddress String?
}

model guest_personal_info {
  id               BigInt    @id(map: "idx_21972_primary") @default(autoincrement())
  firstname        String?   @db.VarChar(255)
  middleinitial    String?   @db.VarChar(255)
  lastname         String?   @db.VarChar(255)
  birthdate        DateTime? @db.Date
  birthplace       String?   @db.VarChar(255)
  citizenship      String?   @db.VarChar(255)
  religion         String?   @db.VarChar(255)
  sex              String?   @db.VarChar(255)
  civilstatus      String?   @db.VarChar(255)
  weight           BigInt?
  height           BigInt?
  currentaddress   String?
  permanentaddress String?
  inputemail       String?   @db.VarChar(255)
  phone            BigInt?
  age              BigInt?
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
}

model guest_tuition {
  id               BigInt    @id(map: "idx_21988_primary") @default(autoincrement())
  totaltuition     BigInt?
  downpayment      BigInt?
  totalbalance     BigInt?
  payment_method   String?   @db.VarChar(255)
  due_date         DateTime? @db.Date
  invoice_uuid     String?   @db.VarChar(255)
  total_lecture    BigInt?
  total_laboratory BigInt?
  miscellaneous    BigInt?
  overall_total    BigInt?
  discount         String?   @db.VarChar(255)
}

model guests_parents_info {
  id          BigInt    @id(map: "idx_21928_primary") @default(autoincrement())
  fathersname String?   @db.VarChar(255)
  mothersname String?   @db.VarChar(255)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
}

model health_check_result_history_items {
  id                   BigInt    @id(map: "idx_21998_primary") @default(autoincrement())
  check_name           String    @db.VarChar(255)
  check_label          String    @db.VarChar(255)
  status               String    @db.VarChar(255)
  notification_message String?
  short_summary        String?   @db.VarChar(255)
  meta                 String
  ended_at             DateTime  @db.Timestamptz(6)
  batch                String    @db.VarChar(255)
  created_at           DateTime? @db.Timestamptz(6)
  updated_at           DateTime? @db.Timestamptz(6)

  @@index([batch], map: "idx_21998_health_check_result_history_items_batch_index")
  @@index([created_at], map: "idx_21998_health_check_result_history_items_created_at_index")
}

model imports {
  id              BigInt    @id(map: "idx_22006_primary") @default(autoincrement())
  completed_at    DateTime? @db.Timestamptz(6)
  file_name       String    @db.VarChar(255)
  file_path       String    @db.VarChar(255)
  importer        String    @db.VarChar(255)
  processed_rows  BigInt    @default(0)
  total_rows      BigInt
  successful_rows BigInt    @default(0)
  user_id         Decimal   @db.Decimal
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model inventory_amendments {
  id                                            BigInt             @id @default(autoincrement())
  product_id                                    BigInt
  recorded_quantity                             Int
  actual_quantity                               Int
  variance                                      Int
  status                                        String             @default("pending") @db.VarChar(255)
  notes                                         String?
  amended_by                                    BigInt
  approved_by                                   BigInt?
  amendment_date                                DateTime           @db.Timestamp(0)
  approved_date                                 DateTime?          @db.Timestamp(0)
  created_at                                    DateTime?          @db.Timestamp(0)
  updated_at                                    DateTime?          @db.Timestamp(0)
  users_inventory_amendments_amended_byTousers  users              @relation("inventory_amendments_amended_byTousers", fields: [amended_by], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "inventory_amendments_amended_by_foreign")
  users_inventory_amendments_approved_byTousers users?             @relation("inventory_amendments_approved_byTousers", fields: [approved_by], references: [id], onUpdate: NoAction, map: "inventory_amendments_approved_by_foreign")
  inventory_products                            inventory_products @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "inventory_amendments_product_id_foreign")

  @@index([product_id, amendment_date], map: "inventory_amendments_product_id_amendment_date_index")
  @@index([status, amendment_date], map: "inventory_amendments_status_amendment_date_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model inventory_borrowings {
  id                                            BigInt             @id @default(autoincrement())
  product_id                                    BigInt
  quantity_borrowed                             Int
  borrower_name                                 String             @db.VarChar(255)
  borrower_email                                String?            @db.VarChar(255)
  borrower_phone                                String?            @db.VarChar(255)
  department                                    String?            @db.VarChar(255)
  purpose                                       String?
  status                                        String             @default("borrowed") @db.VarChar(255)
  borrowed_date                                 DateTime           @db.Timestamp(0)
  expected_return_date                          DateTime?          @db.Timestamp(0)
  actual_return_date                            DateTime?          @db.Timestamp(0)
  quantity_returned                             Int                @default(0)
  return_notes                                  String?
  issued_by                                     BigInt
  returned_to                                   BigInt?
  created_at                                    DateTime?          @db.Timestamp(0)
  updated_at                                    DateTime?          @db.Timestamp(0)
  users_inventory_borrowings_issued_byTousers   users              @relation("inventory_borrowings_issued_byTousers", fields: [issued_by], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "inventory_borrowings_issued_by_foreign")
  inventory_products                            inventory_products @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "inventory_borrowings_product_id_foreign")
  users_inventory_borrowings_returned_toTousers users?             @relation("inventory_borrowings_returned_toTousers", fields: [returned_to], references: [id], onUpdate: NoAction, map: "inventory_borrowings_returned_to_foreign")

  @@index([expected_return_date, status], map: "inventory_borrowings_expected_return_date_status_index")
  @@index([product_id, status], map: "inventory_borrowings_product_id_status_index")
  @@index([status, borrowed_date], map: "inventory_borrowings_status_borrowed_date_index")
}

model inventory_categories {
  id                         BigInt                 @id @default(autoincrement())
  name                       String                 @db.VarChar(255)
  description                String?
  slug                       String                 @unique(map: "inventory_categories_slug_unique") @db.VarChar(255)
  is_active                  Boolean                @default(true)
  parent_id                  BigInt?
  sort_order                 Int                    @default(0)
  created_at                 DateTime?              @db.Timestamp(0)
  updated_at                 DateTime?              @db.Timestamp(0)
  inventory_categories       inventory_categories?  @relation("inventory_categoriesToinventory_categories", fields: [parent_id], references: [id], onUpdate: NoAction, map: "inventory_categories_parent_id_foreign")
  other_inventory_categories inventory_categories[] @relation("inventory_categoriesToinventory_categories")
  inventory_products         inventory_products[]

  @@index([is_active, sort_order], map: "inventory_categories_is_active_sort_order_index")
}

model inventory_products {
  id                        BigInt                      @id @default(autoincrement())
  name                      String                      @db.VarChar(255)
  sku                       String                      @unique(map: "inventory_products_sku_unique") @db.VarChar(255)
  description               String?
  category_id               BigInt?
  supplier_id               BigInt?
  price                     Decimal                     @default(0) @db.Decimal(10, 2)
  cost                      Decimal                     @default(0) @db.Decimal(10, 2)
  stock_quantity            Int                         @default(0)
  min_stock_level           Int                         @default(0)
  max_stock_level           Int?
  unit                      String                      @default("pcs") @db.VarChar(255)
  barcode                   String?                     @db.VarChar(255)
  track_stock               Boolean                     @default(true)
  is_active                 Boolean                     @default(true)
  images                    Json?                       @db.Json
  notes                     String?
  created_at                DateTime?                   @db.Timestamp(0)
  updated_at                DateTime?                   @db.Timestamp(0)
  inventory_amendments      inventory_amendments[]
  inventory_borrowings      inventory_borrowings[]
  inventory_categories      inventory_categories?       @relation(fields: [category_id], references: [id], onUpdate: NoAction, map: "inventory_products_category_id_foreign")
  inventory_suppliers       inventory_suppliers?        @relation(fields: [supplier_id], references: [id], onUpdate: NoAction, map: "inventory_products_supplier_id_foreign")
  inventory_stock_movements inventory_stock_movements[]

  @@index([category_id, is_active], map: "inventory_products_category_id_is_active_index")
  @@index([is_active, sku], map: "inventory_products_is_active_sku_index")
  @@index([stock_quantity], map: "inventory_products_stock_quantity_index")
  @@index([supplier_id, is_active], map: "inventory_products_supplier_id_is_active_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model inventory_stock_movements {
  id                 BigInt             @id @default(autoincrement())
  product_id         BigInt
  type               String             @db.VarChar(255)
  quantity           Int
  previous_stock     Int
  new_stock          Int
  reference          String?            @db.VarChar(255)
  reason             String?
  user_id            BigInt?
  movement_date      DateTime           @db.Timestamp(0)
  created_at         DateTime?          @db.Timestamp(0)
  updated_at         DateTime?          @db.Timestamp(0)
  inventory_products inventory_products @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "inventory_stock_movements_product_id_foreign")
  users              users?             @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "inventory_stock_movements_user_id_foreign")

  @@index([product_id, movement_date], map: "inventory_stock_movements_product_id_movement_date_index")
  @@index([type, movement_date], map: "inventory_stock_movements_type_movement_date_index")
}

model inventory_suppliers {
  id                 BigInt               @id @default(autoincrement())
  name               String               @db.VarChar(255)
  contact_person     String?              @db.VarChar(255)
  email              String?              @db.VarChar(255)
  phone              String?              @db.VarChar(255)
  address            String?
  city               String?              @db.VarChar(255)
  state              String?              @db.VarChar(255)
  postal_code        String?              @db.VarChar(255)
  country            String?              @db.VarChar(255)
  tax_number         String?              @db.VarChar(255)
  notes              String?
  is_active          Boolean              @default(true)
  created_at         DateTime?            @db.Timestamp(0)
  updated_at         DateTime?            @db.Timestamp(0)
  inventory_products inventory_products[]

  @@index([is_active, name], map: "inventory_suppliers_is_active_name_index")
}

model job_batches {
  id             String  @id(map: "idx_22021_primary") @db.VarChar(255)
  name           String  @db.VarChar(255)
  total_jobs     BigInt
  pending_jobs   BigInt
  failed_jobs    BigInt
  failed_job_ids String
  options        String?
  cancelled_at   BigInt?
  created_at     BigInt
  finished_at    BigInt?
}

model jobs {
  id           BigInt  @id(map: "idx_22015_primary") @default(autoincrement())
  queue        String  @db.VarChar(255)
  payload      String
  attempts     BigInt
  reserved_at  BigInt?
  available_at BigInt
  created_at   BigInt

  @@index([queue], map: "idx_22015_jobs_queue_index")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model label_ticket {
  label_id  Decimal @db.Decimal
  ticket_id Decimal @db.Decimal

  @@ignore
}

model labels {
  id         BigInt    @id(map: "idx_22027_primary") @default(autoincrement())
  name       String    @db.VarChar(255)
  slug       String    @db.VarChar(255)
  is_visible Boolean   @default(true)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model layouts {
  id           BigInt    @id(map: "idx_22040_primary") @default(autoincrement())
  user_id      String    @db.VarChar(255)
  layout_title String    @db.VarChar(255)
  layout_slug  String    @db.VarChar(255)
  widgets      String
  created_at   DateTime? @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)
  deleted_at   DateTime? @db.Timestamptz(6)
  is_active    Boolean   @default(true)
}

model library_authors {
  id            BigInt          @id @default(autoincrement())
  name          String          @db.VarChar(255)
  biography     String?
  birth_date    DateTime?       @db.Date
  nationality   String?         @db.VarChar(255)
  created_at    DateTime?       @db.Timestamp(0)
  updated_at    DateTime?       @db.Timestamp(0)
  deleted_at    DateTime?       @db.Timestamp(0)
  library_books library_books[]

  @@index([name], map: "library_authors_name_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model library_books {
  id                     BigInt                   @id @default(autoincrement())
  title                  String                   @db.VarChar(255)
  isbn                   String?                  @unique(map: "library_books_isbn_unique") @db.VarChar(255)
  author_id              BigInt
  category_id            BigInt
  publisher              String?                  @db.VarChar(255)
  publication_year       Int?
  pages                  Int?
  description            String?
  cover_image            String?                  @db.VarChar(255)
  total_copies           Int                      @default(1)
  available_copies       Int                      @default(1)
  location               String?                  @db.VarChar(255)
  status                 String                   @default("available") @db.VarChar(255)
  created_at             DateTime?                @db.Timestamp(0)
  updated_at             DateTime?                @db.Timestamp(0)
  deleted_at             DateTime?                @db.Timestamp(0)
  library_authors        library_authors          @relation(fields: [author_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "library_books_author_id_foreign")
  library_categories     library_categories       @relation(fields: [category_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "library_books_category_id_foreign")
  library_borrow_records library_borrow_records[]

  @@index([isbn], map: "library_books_isbn_index")
  @@index([status], map: "library_books_status_index")
  @@index([title, author_id], map: "library_books_title_author_id_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model library_borrow_records {
  id            BigInt        @id @default(autoincrement())
  book_id       BigInt
  user_id       BigInt
  borrowed_at   DateTime      @db.Timestamp(0)
  due_date      DateTime      @db.Timestamp(0)
  returned_at   DateTime?     @db.Timestamp(0)
  status        String        @default("borrowed") @db.VarChar(255)
  fine_amount   Decimal       @default(0) @db.Decimal(10, 2)
  notes         String?
  created_at    DateTime?     @db.Timestamp(0)
  updated_at    DateTime?     @db.Timestamp(0)
  deleted_at    DateTime?     @db.Timestamp(0)
  library_books library_books @relation(fields: [book_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "library_borrow_records_book_id_foreign")
  users         users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "library_borrow_records_user_id_foreign")

  @@index([book_id, user_id], map: "library_borrow_records_book_id_user_id_index")
  @@index([borrowed_at], map: "library_borrow_records_borrowed_at_index")
  @@index([due_date], map: "library_borrow_records_due_date_index")
  @@index([status], map: "library_borrow_records_status_index")
}

model library_categories {
  id            BigInt          @id @default(autoincrement())
  name          String          @unique(map: "library_categories_name_unique") @db.VarChar(255)
  description   String?
  color         String          @default("#6366f1") @db.VarChar(7)
  created_at    DateTime?       @db.Timestamp(0)
  updated_at    DateTime?       @db.Timestamp(0)
  deleted_at    DateTime?       @db.Timestamp(0)
  library_books library_books[]
}

model login_links {
  id         BigInt    @id @default(autoincrement())
  user_id    BigInt
  token      String    @unique(map: "login_links_token_unique") @db.VarChar(64)
  expires_at DateTime  @db.Timestamp(0)
  used_at    DateTime? @db.Timestamp(0)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  accounts   accounts  @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "login_links_user_id_foreign")
}

model mail_attachments {
  id         BigInt    @id @default(autoincrement())
  mail_id    BigInt
  disk       String    @db.VarChar(255)
  uuid       String    @db.VarChar(255)
  filename   String    @db.VarChar(255)
  mime       String    @db.VarChar(255)
  inline     Boolean
  size       BigInt
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  mails      mails     @relation(fields: [mail_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mail_attachments_mail_id_foreign")
}

model mail_events {
  id              BigInt    @id @default(autoincrement())
  mail_id         BigInt
  type            String    @db.VarChar(255)
  ip_address      String?   @db.VarChar(255)
  hostname        String?   @db.VarChar(255)
  platform        String?   @db.VarChar(255)
  os              String?   @db.VarChar(255)
  browser         String?   @db.VarChar(255)
  user_agent      String?   @db.VarChar(255)
  city            String?   @db.VarChar(255)
  country_code    String?   @db.Char(2)
  link            String?   @db.VarChar(255)
  tag             String?   @db.VarChar(255)
  payload         Json?     @db.Json
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  occurred_at     DateTime? @db.Timestamp(0)
  unsuppressed_at DateTime? @db.Timestamp(0)
  mails           mails     @relation(fields: [mail_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mail_events_mail_id_foreign")
}

model mail_logs {
  id          BigInt    @id(map: "idx_22048_primary") @default(autoincrement())
  from        String?   @db.VarChar(255)
  to          String?   @db.VarChar(255)
  cc          String?   @db.VarChar(255)
  bcc         String?   @db.VarChar(255)
  subject     String    @db.VarChar(255)
  body        String
  headers     String?
  attachments String?
  message_id  String?   @db.VarChar(255)
  status      String?   @db.VarChar(255)
  data        String?
  opened      DateTime? @db.Timestamptz(6)
  delivered   DateTime? @db.Timestamptz(6)
  complaint   DateTime? @db.Timestamptz(6)
  bounced     DateTime? @db.Timestamptz(6)
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)

  @@index([message_id], map: "idx_22048_mail_logs_message_id_index")
  @@index([status], map: "idx_22048_mail_logs_status_index")
}

model mailables {
  id            BigInt @id @default(autoincrement())
  mail_id       BigInt
  mailable_type String @db.VarChar(255)
  mailable_id   BigInt
  mails         mails  @relation(fields: [mail_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mailables_mail_id_foreign")

  @@index([mailable_type, mailable_id], map: "mailables_mailable_type_mailable_id_index")
}

model mails {
  id               BigInt             @id @default(autoincrement())
  uuid             String?            @db.VarChar(255)
  mail_class       String?            @db.VarChar(255)
  subject          String?            @db.VarChar(255)
  from             Json?              @db.Json
  reply_to         Json?              @db.Json
  to               Json?              @db.Json
  cc               Json?              @db.Json
  bcc              Json?              @db.Json
  html             String?
  text             String?
  opens            BigInt             @default(0)
  clicks           BigInt             @default(0)
  sent_at          DateTime?          @db.Timestamp(0)
  resent_at        DateTime?          @db.Timestamp(0)
  accepted_at      DateTime?          @db.Timestamp(0)
  delivered_at     DateTime?          @db.Timestamp(0)
  last_opened_at   DateTime?          @db.Timestamp(0)
  last_clicked_at  DateTime?          @db.Timestamp(0)
  complained_at    DateTime?          @db.Timestamp(0)
  soft_bounced_at  DateTime?          @db.Timestamp(0)
  hard_bounced_at  DateTime?          @db.Timestamp(0)
  unsubscribed_at  DateTime?          @db.Timestamp(0)
  created_at       DateTime?          @db.Timestamp(0)
  updated_at       DateTime?          @db.Timestamp(0)
  mailer           String             @db.VarChar(255)
  stream_id        String?            @db.VarChar(255)
  transport        String?            @db.VarChar(255)
  mail_attachments mail_attachments[]
  mail_events      mail_events[]
  mailables        mailables[]

  @@index([mail_class], map: "mails_mail_class_index")
  @@index([uuid], map: "mails_uuid_index")
}

model media {
  id                    BigInt    @id(map: "idx_22061_primary") @default(autoincrement())
  model_type            String    @db.VarChar(255)
  model_id              Decimal   @db.Decimal
  uuid                  String?   @unique(map: "idx_22061_media_uuid_unique") @db.Char(36)
  collection_name       String    @db.VarChar(255)
  name                  String    @db.VarChar(255)
  file_name             String    @db.VarChar(255)
  mime_type             String?   @db.VarChar(255)
  disk                  String    @db.VarChar(255)
  conversions_disk      String?   @db.VarChar(255)
  size                  Decimal   @db.Decimal
  manipulations         String
  custom_properties     String
  generated_conversions String
  responsive_images     String
  order_column          BigInt?
  created_at            DateTime? @db.Timestamptz(6)
  updated_at            DateTime? @db.Timestamptz(6)

  @@index([model_type, model_id], map: "idx_22061_media_model_type_model_id_index")
  @@index([order_column], map: "idx_22061_media_order_column_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model medical_records {
  id                             BigInt    @id @default(autoincrement())
  student_id                     BigInt
  record_type                    String    @db.VarChar(255)
  title                          String    @db.VarChar(255)
  description                    String?
  diagnosis                      String?
  treatment                      String?
  prescription                   String?
  notes                          String?
  doctor_name                    String?   @db.VarChar(255)
  clinic_name                    String?   @db.VarChar(255)
  clinic_address                 String?   @db.VarChar(255)
  doctor_contact                 String?   @db.VarChar(255)
  visit_date                     DateTime  @db.Date
  next_appointment               DateTime? @db.Date
  follow_up_date                 DateTime? @db.Date
  status                         String    @default("active") @db.VarChar(255)
  priority                       String    @default("normal") @db.VarChar(255)
  is_confidential                Boolean   @default(false)
  height                         Decimal?  @db.Decimal(5, 2)
  weight                         Decimal?  @db.Decimal(5, 2)
  blood_pressure_systolic        Int?
  blood_pressure_diastolic       Int?
  temperature                    Decimal?  @db.Decimal(4, 1)
  heart_rate                     Int?
  bmi                            Decimal?  @db.Decimal(4, 1)
  vital_signs                    Json?     @db.Json
  lab_results                    Json?     @db.Json
  attachments                    Json?     @db.Json
  emergency_contact_notified     Boolean   @default(false)
  emergency_notification_sent_at DateTime? @db.Timestamp(0)
  created_by                     BigInt?
  updated_by                     BigInt?
  created_at                     DateTime? @db.Timestamp(0)
  updated_at                     DateTime? @db.Timestamp(0)
  deleted_at                     DateTime? @db.Timestamp(0)

  @@index([is_confidential], map: "medical_records_is_confidential_index")
  @@index([status, priority], map: "medical_records_status_priority_index")
  @@index([student_id, record_type], map: "medical_records_student_id_record_type_index")
  @@index([student_id, visit_date], map: "medical_records_student_id_visit_date_index")
  @@index([visit_date], map: "medical_records_visit_date_index")
}

model messages {
  id         BigInt    @id(map: "idx_22071_primary") @default(autoincrement())
  user_id    Decimal   @db.Decimal
  ticket_id  Decimal   @db.Decimal
  message    String
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model migrations {
  id        BigInt @id(map: "idx_22078_primary") @default(autoincrement())
  migration String @db.VarChar(255)
  batch     BigInt
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model missing_student_requests {
  id           BigInt    @id @default(autoincrement())
  class_id     Int
  faculty_id   String    @db.Uuid
  full_name    String    @db.VarChar(255)
  student_id   String?   @db.VarChar(255)
  email        String?   @db.VarChar(255)
  notes        String?
  status       String    @default("pending") @db.VarChar(255)
  submitted_at DateTime  @db.Timestamp(0)
  processed_at DateTime? @db.Timestamp(0)
  processed_by BigInt?
  admin_notes  String?
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  classes      classes   @relation(fields: [class_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "missing_student_requests_class_id_foreign")
  faculty      faculty   @relation(fields: [faculty_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "missing_student_requests_faculty_id_foreign")
  accounts     accounts? @relation(fields: [processed_by], references: [id], onUpdate: NoAction, map: "missing_student_requests_processed_by_foreign")

  @@index([class_id, status], map: "missing_student_requests_class_id_status_index")
  @@index([faculty_id, status], map: "missing_student_requests_faculty_id_status_index")
  @@index([status], map: "missing_student_requests_status_index")
}

model model_has_permissions {
  permission_id BigInt
  model_type    String      @db.VarChar(255)
  model_id      BigInt
  permissions   permissions @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "model_has_permissions_permission_id_foreign")

  @@id([permission_id, model_id, model_type])
  @@index([model_id, model_type], map: "model_has_permissions_model_id_model_type_index")
}

model model_has_roles {
  role_id    BigInt
  model_type String @db.VarChar(255)
  model_id   BigInt
  roles      roles  @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "model_has_roles_role_id_foreign")

  @@id([role_id, model_id, model_type])
  @@index([model_id, model_type], map: "model_has_roles_model_id_model_type_index")
}

model notification_campaigns {
  id                                                   BigInt    @id @default(autoincrement())
  title                                                String    @db.VarChar(255)
  description                                          String?
  type                                                 String    @db.VarChar(255)
  priority                                             String    @default("normal") @db.VarChar(255)
  status                                               String    @default("draft") @db.VarChar(255)
  notification_title                                   String    @db.VarChar(255)
  notification_message                                 String
  action_text                                          String?   @db.VarChar(255)
  action_url                                           String?   @db.VarChar(255)
  additional_data                                      Json?     @db.Json
  recipient_ids                                        Json?     @db.Json
  send_to_all_faculty                                  Boolean   @default(false)
  recipient_filters                                    Json?     @db.Json
  scheduled_at                                         DateTime? @db.Timestamp(0)
  sent_at                                              DateTime? @db.Timestamp(0)
  expires_at                                           DateTime? @db.Timestamp(0)
  total_recipients                                     Int       @default(0)
  sent_count                                           Int       @default(0)
  failed_count                                         Int       @default(0)
  error_log                                            Json?     @db.Json
  created_by                                           BigInt
  updated_by                                           BigInt?
  created_at                                           DateTime? @db.Timestamp(0)
  updated_at                                           DateTime? @db.Timestamp(0)
  send_email                                           Boolean   @default(false)
  email_subject                                        String?   @db.VarChar(255)
  email_message                                        String?
  email_sent_count                                     Int       @default(0)
  email_failed_count                                   Int       @default(0)
  accounts_notification_campaigns_created_byToaccounts accounts  @relation("notification_campaigns_created_byToaccounts", fields: [created_by], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "notification_campaigns_created_by_foreign")
  accounts_notification_campaigns_updated_byToaccounts accounts? @relation("notification_campaigns_updated_byToaccounts", fields: [updated_by], references: [id], onUpdate: NoAction, map: "notification_campaigns_updated_by_foreign")

  @@index([created_by], map: "notification_campaigns_created_by_index")
  @@index([scheduled_at], map: "notification_campaigns_scheduled_at_index")
  @@index([type, status], map: "notification_campaigns_type_status_index")
}

model notifications {
  id              String    @id(map: "idx_22092_primary") @db.VarChar(255)
  type            String    @db.VarChar(255)
  notifiable_type String    @db.VarChar(255)
  notifiable_id   BigInt
  data            Json
  read_at         DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@index([notifiable_type, notifiable_id], map: "idx_22092_notifications_notifiable_type_notifiable_id_index")
}

model oauth_connections {
  id            BigInt    @id @default(autoincrement())
  user_id       BigInt
  provider      String    @db.VarChar(255)
  provider_id   String    @db.VarChar(255)
  data          Json?     @db.Json
  token         String?   @db.VarChar(255)
  refresh_token String?   @db.VarChar(255)
  expires_at    DateTime? @db.Timestamp(0)
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  accounts      accounts  @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "oauth_connections_user_id_foreign")

  @@unique([user_id, provider], map: "oauth_connections_user_id_provider_unique")
}

model old_user_emails {
  id         BigInt    @id(map: "idx_22098_primary") @default(autoincrement())
  user_type  String    @db.VarChar(255)
  user_id    Decimal   @db.Decimal
  email      String    @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)

  @@index([email], map: "idx_22098_old_user_emails_email_index")
  @@index([user_type, user_id], map: "idx_22098_old_user_emails_user_type_user_id_index")
}

model passkeys {
  id                 BigInt    @id @default(autoincrement())
  authenticatable_id BigInt
  name               String
  credential_id      String
  data               Json      @db.Json
  last_used_at       DateTime? @db.Timestamp(0)
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  users              users     @relation(fields: [authenticatable_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "passkeys_authenticatable_fk")
}

model password_reset_tokens {
  email      String    @id(map: "idx_22104_primary") @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
}

model pending_enrollments {
  id           BigInt    @id @default(autoincrement())
  data         Json      @db.Json
  status       String    @default("pending") @db.VarChar(255)
  remarks      String?
  approved_by  BigInt?
  processed_at DateTime? @db.Timestamp(0)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  users        users?    @relation(fields: [approved_by], references: [id], onUpdate: NoAction, map: "pending_enrollments_approved_by_foreign")

  @@index([status], map: "pending_enrollments_status_index")
}

model pending_user_emails {
  id         BigInt    @id(map: "idx_22110_primary") @default(autoincrement())
  user_type  String    @db.VarChar(255)
  user_id    Decimal   @db.Decimal
  email      String    @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)

  @@index([email], map: "idx_22110_pending_user_emails_email_index")
  @@index([user_type, user_id], map: "idx_22110_pending_user_emails_user_type_user_id_index")
}

model permission_type {
  id               BigInt  @id(map: "idx_22124_primary") @default(autoincrement())
  permission_array BigInt?
}

model permissions {
  id                    BigInt                  @id @default(autoincrement())
  name                  String                  @db.VarChar(255)
  guard_name            String                  @db.VarChar(255)
  created_at            DateTime?               @db.Timestamp(0)
  updated_at            DateTime?               @db.Timestamp(0)
  model_has_permissions model_has_permissions[]
  role_has_permissions  role_has_permissions[]

  @@unique([name, guard_name], map: "permissions_name_guard_name_unique")
}

model personal_access_tokens {
  id             BigInt    @id(map: "idx_22129_primary") @default(autoincrement())
  tokenable_type String    @db.VarChar(255)
  tokenable_id   Decimal   @db.Decimal
  name           String    @db.VarChar(255)
  token          String    @unique(map: "idx_22129_personal_access_tokens_token_unique") @db.VarChar(64)
  abilities      String?
  last_used_at   DateTime? @db.Timestamptz(6)
  expires_at     DateTime? @db.Timestamptz(6)
  created_at     DateTime? @db.Timestamptz(6)
  updated_at     DateTime? @db.Timestamptz(6)

  @@index([tokenable_type, tokenable_id], map: "idx_22129_personal_access_tokens_tokenable_type_tokenable_id_in")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model private_beta_invitations {
  id             BigInt    @id @default(autoincrement())
  email          String    @db.VarChar(255)
  status         String    @default("pending") @db.VarChar(16)
  access_code    String    @db.VarChar(32)
  expire_at      DateTime? @db.Timestamp(0)
  num_requests   Int       @default(0)
  last_access_at DateTime? @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)

  @@index([access_code], map: "private_beta_invitations_access_code_index")
  @@index([email], map: "private_beta_invitations_email_index")
  @@index([status], map: "private_beta_invitations_status_index")
}

model process_approval_flow_steps {
  id                       BigInt                             @id(map: "idx_22136_primary") @default(autoincrement())
  process_approval_flow_id Decimal                            @db.Decimal
  role_id                  Decimal                            @db.Decimal
  permissions              String?
  order                    BigInt?
  action                   process_approval_flow_steps_action @default(APPROVE)
  active                   Int                                @default(1) @db.SmallInt
  created_at               DateTime?                          @db.Timestamptz(6)
  updated_at               DateTime?                          @db.Timestamptz(6)
}

model projects {
  id          BigInt    @id(map: "idx_22145_primary") @default(autoincrement())
  name        String    @db.VarChar(255)
  hourly_rate Decimal   @db.Decimal(8, 2)
  client_id   Decimal   @db.Decimal
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)

  @@index([client_id], map: "idx_22145_projects_client_id_foreign")
}

model pulse_aggregates {
  id        BigInt  @id @default(autoincrement())
  bucket    Int
  period    Int
  type      String  @db.VarChar(255)
  key       String
  key_hash  String  @default(dbgenerated("(md5(key))::uuid")) @db.Uuid
  aggregate String  @db.VarChar(255)
  value     Decimal @db.Decimal(20, 2)
  count     Int?

  @@unique([bucket, period, type, aggregate, key_hash], map: "pulse_aggregates_bucket_period_type_aggregate_key_hash_unique")
  @@index([period, bucket], map: "pulse_aggregates_period_bucket_index")
  @@index([period, type, aggregate, bucket], map: "pulse_aggregates_period_type_aggregate_bucket_index")
  @@index([type], map: "pulse_aggregates_type_index")
}

model pulse_entries {
  id        BigInt  @id @default(autoincrement())
  timestamp Int
  type      String  @db.VarChar(255)
  key       String
  key_hash  String  @default(dbgenerated("(md5(key))::uuid")) @db.Uuid
  value     BigInt?

  @@index([key_hash], map: "pulse_entries_key_hash_index")
  @@index([timestamp], map: "pulse_entries_timestamp_index")
  @@index([timestamp, type, key_hash, value], map: "pulse_entries_timestamp_type_key_hash_value_index")
  @@index([type], map: "pulse_entries_type_index")
}

model pulse_values {
  id        BigInt @id @default(autoincrement())
  timestamp Int
  type      String @db.VarChar(255)
  key       String
  key_hash  String @default(dbgenerated("(md5(key))::uuid")) @db.Uuid
  value     String

  @@unique([type, key_hash], map: "pulse_values_type_key_hash_unique")
  @@index([timestamp], map: "pulse_values_timestamp_index")
  @@index([type], map: "pulse_values_type_index")
}

model push_subscriptions {
  id                BigInt    @id @default(autoincrement())
  subscribable_type String    @db.VarChar(255)
  subscribable_id   BigInt
  endpoint          String    @unique(map: "push_subscriptions_endpoint_unique") @db.VarChar(500)
  public_key        String?   @db.VarChar(255)
  auth_token        String?   @db.VarChar(255)
  content_encoding  String?   @db.VarChar(255)
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)

  @@index([subscribable_type, subscribable_id], map: "push_subscriptions_subscribable_type_subscribable_id_index")
}

model resource_bookings {
  id                    BigInt     @id @default(autoincrement())
  resource_type         String     @db.VarChar(255)
  resource_name         String     @db.VarChar(255)
  resource_description  String?
  location              String?    @db.VarChar(255)
  capacity              Int?
  features              Json?      @db.Json
  hourly_rate           Decimal?   @db.Decimal(8, 2)
  availability_schedule Json?      @db.Json
  requires_approval     Boolean    @default(false)
  is_active             Boolean    @default(true)
  booking_rules         Json?      @db.Json
  terms_and_conditions  String?
  managed_by            BigInt?
  created_at            DateTime?  @db.Timestamp(0)
  updated_at            DateTime?  @db.Timestamp(0)
  bookings              bookings[]
  users                 users?     @relation(fields: [managed_by], references: [id], onUpdate: NoAction, map: "resource_bookings_managed_by_foreign")

  @@index([location, resource_type], map: "resource_bookings_location_resource_type_index")
  @@index([resource_type, is_active], map: "resource_bookings_resource_type_is_active_index")
}

model resource_logs {
  id         BigInt    @id(map: "idx_22173_primary") @default(autoincrement())
  resource   String    @db.VarChar(255)
  log        String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model resources {
  id                BigInt    @id @default(autoincrement())
  resourceable_type String    @db.VarChar(255)
  resourceable_id   BigInt
  type              String    @db.VarChar(255)
  file_path         String    @db.VarChar(255)
  file_name         String    @db.VarChar(255)
  mime_type         String?   @db.VarChar(255)
  disk              String    @default("public") @db.VarChar(255)
  file_size         BigInt?
  metadata          Json?     @db.Json
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)

  @@index([resourceable_type, resourceable_id], map: "resources_resourceable_type_resourceable_id_index")
  @@index([resourceable_type, resourceable_id, type], map: "resources_resourceable_type_resourceable_id_type_index")
}

model role_has_permissions {
  permission_id BigInt
  role_id       BigInt
  permissions   permissions @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "role_has_permissions_permission_id_foreign")
  roles         roles       @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "role_has_permissions_role_id_foreign")

  @@id([permission_id, role_id])
}

model roles {
  id                   BigInt                 @id @default(autoincrement())
  name                 String                 @db.VarChar(255)
  guard_name           String                 @db.VarChar(255)
  created_at           DateTime?              @db.Timestamp(0)
  updated_at           DateTime?              @db.Timestamp(0)
  model_has_roles      model_has_roles[]
  role_has_permissions role_has_permissions[]

  @@unique([name, guard_name], map: "roles_name_guard_name_unique")
}

model rooms {
  id         BigInt    @id(map: "idx_22192_primary") @default(autoincrement())
  name       String    @db.VarChar(255)
  class_code String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model route_statistics {
  id      BigInt   @id(map: "idx_22199_primary") @default(autoincrement())
  user_id BigInt?
  team_id BigInt?
  method  String?  @db.VarChar(255)
  route   String?  @db.VarChar(255)
  status  BigInt?
  ip      String?  @db.VarChar(255)
  date    DateTime @db.Timestamptz(6)
  counter BigInt
  users   users?   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "route_statistics_user_id_foreign")

  @@index([date], map: "idx_22199_route_statistics_date_index")
  @@index([route, method, date], map: "idx_22199_route_statistics_route_method_date_index")
  @@index([team_id, date, route, method], map: "idx_22199_route_statistics_team_id_date_route_method_index")
  @@index([user_id, date, route, method], map: "idx_22199_route_statistics_user_id_date_route_method_index")
}

model routes {
  id                                   BigInt    @id @default(autoincrement())
  name                                 String    @unique(map: "routes_name_unique") @db.VarChar(255)
  display_name                         String    @db.VarChar(255)
  path                                 String    @db.VarChar(255)
  method                               String    @default("GET") @db.VarChar(255)
  controller                           String?   @db.VarChar(255)
  action                               String?   @db.VarChar(255)
  type                                 String    @db.VarChar(255)
  status                               String    @default("active") @db.VarChar(255)
  description                          String?
  icon                                 String?   @db.VarChar(255)
  sort_order                           Int       @default(0)
  parent_id                            BigInt?
  is_navigation                        Boolean   @default(false)
  is_mobile_visible                    Boolean   @default(true)
  is_desktop_visible                   Boolean   @default(true)
  required_permissions                 Json?     @db.Json
  middleware                           Json?     @db.Json
  parameters                           Json?     @db.Json
  metadata                             Json?     @db.Json
  disabled_message                     String?
  maintenance_message                  String?
  development_message                  String?
  redirect_url                         String?   @db.VarChar(255)
  is_external                          Boolean   @default(false)
  target                               String    @default("_self") @db.VarChar(255)
  created_by                           BigInt?
  updated_by                           BigInt?
  disabled_at                          DateTime? @db.Timestamp(0)
  enabled_at                           DateTime? @db.Timestamp(0)
  created_at                           DateTime? @db.Timestamp(0)
  updated_at                           DateTime? @db.Timestamp(0)
  deleted_at                           DateTime? @db.Timestamp(0)
  accounts_routes_created_byToaccounts accounts? @relation("routes_created_byToaccounts", fields: [created_by], references: [id], onUpdate: NoAction, map: "routes_created_by_foreign")
  routes                               routes?   @relation("routesToroutes", fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "routes_parent_id_foreign")
  other_routes                         routes[]  @relation("routesToroutes")
  accounts_routes_updated_byToaccounts accounts? @relation("routes_updated_byToaccounts", fields: [updated_by], references: [id], onUpdate: NoAction, map: "routes_updated_by_foreign")

  @@index([is_desktop_visible, status], map: "routes_is_desktop_visible_status_index")
  @@index([is_mobile_visible, status], map: "routes_is_mobile_visible_status_index")
  @@index([parent_id, sort_order], map: "routes_parent_id_sort_order_index")
  @@index([status, is_navigation], map: "routes_status_is_navigation_index")
  @@index([type, is_navigation, status], map: "routes_type_is_navigation_status_index")
  @@index([type, status], map: "routes_type_status_index")
}

model schedule {
  id          BigInt    @id(map: "idx_22209_primary") @default(autoincrement())
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at  DateTime? @db.Timestamptz(6)
  day_of_week String    @db.VarChar(255)
  start_time  DateTime  @db.Time(6)
  end_time    DateTime  @db.Time(6)
  class_id    BigInt?
  room_id     BigInt?
  updated_at  DateTime? @db.Timestamptz(6)
}

model schools {
  id          BigInt        @id @default(autoincrement())
  name        String        @unique(map: "schools_name_unique") @db.VarChar(255)
  code        String        @unique(map: "schools_code_unique") @db.VarChar(10)
  description String?
  dean_name   String?       @db.VarChar(255)
  dean_email  String?       @db.VarChar(255)
  location    String?       @db.VarChar(255)
  phone       String?       @db.VarChar(255)
  email       String?       @db.VarChar(255)
  is_active   Boolean       @default(true)
  metadata    Json?         @db.Json
  created_at  DateTime?     @db.Timestamp(0)
  updated_at  DateTime?     @db.Timestamp(0)
  departments departments[]
  users       users[]

  @@index([code], map: "schools_code_index")
  @@index([is_active], map: "schools_is_active_index")
}

model seo {
  id            BigInt    @id(map: "idx_22215_primary") @default(autoincrement())
  model_type    String    @db.VarChar(255)
  model_id      Decimal   @db.Decimal
  description   String?
  title         String?   @db.VarChar(255)
  image         String?   @db.VarChar(255)
  author        String?   @db.VarChar(255)
  robots        String?   @db.VarChar(255)
  canonical_url String?   @db.VarChar(255)
  created_at    DateTime? @db.Timestamptz(6)
  updated_at    DateTime? @db.Timestamptz(6)

  @@index([model_type, model_id], map: "idx_22215_seo_model_type_model_id_index")
}

model server_metrics {
  id           BigInt    @id(map: "idx_22227_primary") @default(autoincrement())
  cpu_load     BigInt
  memory_usage BigInt
  disk_usage   String
  site_id      BigInt
  created_at   DateTime? @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)

  @@index([site_id], map: "idx_22227_server_metrics_site_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model sessions {
  id            String  @db.VarChar(255)
  user_id       BigInt?
  ip_address    String? @db.VarChar(255)
  user_agent    String?
  payload       String
  last_activity BigInt

  @@index([last_activity], map: "idx_22233_sessions_last_activity_index")
  @@index([user_id], map: "idx_22233_sessions_user_id_index")
  @@ignore
}

model settings {
  id         BigInt    @id @default(autoincrement())
  group      String    @db.VarChar(255)
  name       String    @db.VarChar(255)
  locked     Boolean   @default(false)
  payload    Json      @db.Json
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@unique([group, name], map: "settings_group_name_unique")
}

model shetabit_visits {
  id             BigInt    @id(map: "idx_22248_primary") @default(autoincrement())
  method         String?   @db.VarChar(255)
  request        String?
  url            String?
  referer        String?
  languages      String?
  useragent      String?
  headers        String?
  device         String?
  platform       String?
  browser        String?
  ip             String?   @db.VarChar(45)
  visitable_type String?   @db.VarChar(255)
  visitable_id   Decimal?  @db.Decimal
  visitor_type   String?   @db.VarChar(255)
  visitor_id     Decimal?  @db.Decimal
  created_at     DateTime? @db.Timestamptz(6)
  updated_at     DateTime? @db.Timestamptz(6)

  @@index([visitable_type, visitable_id], map: "idx_22248_shetabit_visits_visitable_type_visitable_id_index")
  @@index([visitor_type, visitor_id], map: "idx_22248_shetabit_visits_visitor_type_visitor_id_index")
}

model shs_strands {
  id          BigInt     @id @default(autoincrement())
  track_id    BigInt
  strand_name String     @db.VarChar(255)
  description String?
  created_at  DateTime?  @db.Timestamp(0)
  updated_at  DateTime?  @db.Timestamp(0)
  classes     classes[]
  shs_tracks  shs_tracks @relation(fields: [track_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "shs_strands_track_id_foreign")
}

model shs_students {
  id               BigInt    @id(map: "idx_22259_primary") @default(autoincrement())
  student_lrn      String?   @db.VarChar(255)
  fullname         String?   @db.VarChar(255)
  civil_status     String?   @db.VarChar(255)
  religion         String?   @db.VarChar(255)
  nationality      String?   @db.VarChar(255)
  birthdate        String?   @db.VarChar(11)
  guardian_name    String?   @db.VarChar(255)
  guardian_contact String?   @db.VarChar(255)
  student_contact  String?   @db.VarChar(255)
  complete_address String?   @db.VarChar(255)
  grade_level      String?   @db.VarChar(255)
  track            String?   @db.VarChar(255)
  gender           String?   @db.VarChar(255)
  email            String?   @db.VarChar(255)
  remarks          String?   @db.VarChar(255)
  created_at       DateTime? @db.Timestamptz(6)
  updated_at       DateTime? @db.Timestamptz(6)
  strand_id        BigInt?
  track_id         BigInt?
}

model shs_tracks {
  id          BigInt        @id(map: "idx_22281_primary") @default(autoincrement())
  track_name  String        @db.VarChar(255)
  description String?       @db.VarChar(255)
  created_at  DateTime?     @db.Timestamptz(6)
  updated_at  DateTime?     @db.Timestamptz(6)
  classes     classes[]
  shs_strands shs_strands[]
}

model sites {
  id                                     BigInt    @id(map: "idx_22289_primary") @default(autoincrement())
  url                                    String    @unique(map: "idx_22289_sites_url_unique") @db.VarChar(255)
  name                                   String    @db.VarChar(255)
  uptime_check_enabled                   Boolean   @default(true)
  ssl_certificate_check_enabled          Boolean   @default(true)
  max_request_duration_ms                BigInt    @default(1000)
  down_for_maintenance_at                DateTime? @db.Timestamptz(6)
  server_monitoring_notification_enabled Boolean   @default(false)
  cpu_limit                              BigInt?
  ram_limit                              BigInt?
  disk_limit                             BigInt?
  api_token                              String?   @db.VarChar(255)
  created_at                             DateTime? @db.Timestamptz(6)
  updated_at                             DateTime? @db.Timestamptz(6)
}

model social_provider_user {
  user_id          BigInt
  provider_slug    String    @db.VarChar(255)
  provider_user_id String    @db.VarChar(255)
  nickname         String?   @db.VarChar(255)
  name             String?   @db.VarChar(255)
  email            String?   @db.VarChar(255)
  avatar           String?   @db.VarChar(255)
  provider_data    String?
  token            String    @db.VarChar(255)
  refresh_token    String?   @db.VarChar(255)
  token_expires_at DateTime? @db.Timestamptz(6)
  created_at       DateTime? @db.Timestamptz(6)
  updated_at       DateTime? @db.Timestamptz(6)

  @@id([user_id, provider_slug], map: "idx_22300_primary")
}

model ssl_certificate_checks {
  id                   BigInt    @id(map: "idx_22311_primary") @default(autoincrement())
  status               String    @default("not yet checked") @db.VarChar(255)
  issuer               String?   @db.VarChar(255)
  expiration_date      DateTime? @db.Timestamptz(6)
  check_failure_reason String?   @db.VarChar(255)
  site_id              BigInt
  created_at           DateTime? @db.Timestamptz(6)
  updated_at           DateTime? @db.Timestamptz(6)

  @@index([site_id], map: "idx_22311_ssl_certificate_checks_site_id_foreign")
}

model strand_subjects {
  id          BigInt    @id(map: "idx_22321_primary") @default(autoincrement())
  code        String    @db.VarChar(255)
  title       String    @db.VarChar(255)
  description String?   @db.VarChar(255)
  grade_year  String    @db.VarChar(255)
  semester    String    @db.VarChar(255)
  strand_id   BigInt
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

model student_clearances {
  id            BigInt    @id @default(autoincrement())
  student_id    BigInt
  academic_year String    @db.VarChar(255)
  semester      Int       @db.SmallInt
  is_cleared    Boolean   @default(false)
  remarks       String?
  cleared_by    String?   @db.VarChar(255)
  cleared_at    DateTime? @db.Timestamp(0)
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  students      students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_clearances_student_id_foreign")

  @@unique([student_id, academic_year, semester], map: "student_clearances_student_id_academic_year_semester_unique")
  @@index([is_cleared], map: "idx_student_clearances_is_cleared")
  @@index([student_id, academic_year, semester], map: "idx_student_clearances_lookup")
  @@index([academic_year, semester], map: "idx_student_clearances_period")
  @@index([student_id], map: "idx_student_clearances_student_id")
}

model student_contacts {
  id                        BigInt    @id(map: "idx_22359_primary") @default(autoincrement())
  emergency_contact_name    String?   @db.VarChar(255)
  emergency_contact_phone   String?   @db.VarChar(255)
  emergency_contact_address String?   @db.VarChar(255)
  facebook_contact          String?   @db.VarChar(255)
  personal_contact          String?   @db.VarChar(255)
  created_at                DateTime? @db.Timestamp(0)
  updated_at                DateTime? @db.Timestamp(0)
  student_id                BigInt?
}

model student_education_info {
  id                          BigInt  @id(map: "idx_22371_primary") @default(autoincrement())
  elementary_school           String? @db.VarChar(255)
  elementary_graduate_year    BigInt?
  senior_high_name            String? @db.VarChar(255)
  senior_high_graduate_year   BigInt?
  elementary_school_address   String? @db.VarChar(255)
  senior_high_address         String? @db.VarChar(255)
  junior_high_school_name     String? @db.VarChar(255)
  junior_high_school_address  String? @db.VarChar(255)
  junior_high_graduation_year String? @db.VarChar(255)
}

model student_enrollment {
  id              BigInt            @id(map: "idx_22385_primary") @default(autoincrement())
  student_id      String            @db.VarChar(255)
  course_id       String            @db.VarChar(255)
  status          String            @db.VarChar(255)
  created_at      DateTime?         @db.Timestamptz(6)
  updated_at      DateTime?         @db.Timestamptz(6)
  semester        BigInt?
  academic_year   BigInt?
  school_year     String?           @db.VarChar(255)
  deleted_at      DateTime?         @db.Timestamptz(6)
  downpayment     Float?            @default(0) @db.Real
  remarks         String?
  payment_method  String?           @db.VarChar(255)
  additional_fees additional_fees[]

  @@index([created_at], map: "idx_created_at")
  @@index([deleted_at], map: "idx_deleted_at")
  @@index([created_at], map: "idx_student_enrollment_created_at")
  @@index([student_id, school_year, semester, status], map: "idx_student_enrollment_period")
  @@index([school_year], map: "idx_student_enrollment_school_year")
  @@index([semester, school_year], map: "idx_student_enrollment_semester")
  @@index([status], map: "idx_student_enrollment_status")
}

model student_id_change_logs {
  id               BigInt    @id @default(autoincrement())
  old_student_id   String    @db.VarChar(255)
  new_student_id   String    @db.VarChar(255)
  student_name     String    @db.VarChar(255)
  changed_by       String    @db.VarChar(255)
  affected_records Json      @db.Json
  backup_data      Json?     @db.Json
  is_undone        Boolean   @default(false)
  undone_at        DateTime? @db.Timestamp(0)
  undone_by        String?   @db.VarChar(255)
  reason           String?
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)

  @@index([changed_by], map: "student_id_change_logs_changed_by_index")
  @@index([is_undone], map: "student_id_change_logs_is_undone_index")
  @@index([old_student_id, new_student_id], map: "student_id_change_logs_old_student_id_new_student_id_index")
}

model student_notification_campaigns {
  id                                                           BigInt    @id @default(autoincrement())
  title                                                        String    @db.VarChar(255)
  description                                                  String?
  type                                                         String    @db.VarChar(255)
  priority                                                     String    @default("normal") @db.VarChar(255)
  status                                                       String    @default("draft") @db.VarChar(255)
  notification_title                                           String    @db.VarChar(255)
  notification_message                                         String
  action_text                                                  String?   @db.VarChar(255)
  action_url                                                   String?   @db.VarChar(255)
  additional_data                                              Json?     @db.Json
  recipient_ids                                                Json?     @db.Json
  send_to_all_students                                         Boolean   @default(false)
  recipient_filters                                            Json?     @db.Json
  scheduled_at                                                 DateTime? @db.Timestamp(0)
  sent_at                                                      DateTime? @db.Timestamp(0)
  expires_at                                                   DateTime? @db.Timestamp(0)
  total_recipients                                             Int       @default(0)
  sent_count                                                   Int       @default(0)
  failed_count                                                 Int       @default(0)
  error_log                                                    Json?     @db.Json
  created_by                                                   BigInt
  updated_by                                                   BigInt?
  created_at                                                   DateTime? @db.Timestamp(0)
  updated_at                                                   DateTime? @db.Timestamp(0)
  send_email                                                   Boolean   @default(false)
  email_subject                                                String?   @db.VarChar(255)
  email_message                                                String?
  email_sent_count                                             Int       @default(0)
  email_failed_count                                           Int       @default(0)
  accounts_student_notification_campaigns_created_byToaccounts accounts  @relation("student_notification_campaigns_created_byToaccounts", fields: [created_by], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_notification_campaigns_created_by_foreign")
  accounts_student_notification_campaigns_updated_byToaccounts accounts? @relation("student_notification_campaigns_updated_byToaccounts", fields: [updated_by], references: [id], onUpdate: NoAction, map: "student_notification_campaigns_updated_by_foreign")

  @@index([created_by], map: "student_notification_campaigns_created_by_index")
  @@index([scheduled_at], map: "student_notification_campaigns_scheduled_at_index")
  @@index([type, status], map: "student_notification_campaigns_type_status_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model student_notifications {
  id          BigInt    @id @default(autoincrement())
  user_id     BigInt
  title       String    @db.VarChar(255)
  message     String
  type        String    @default("info") @db.VarChar(255)
  data        Json?     @db.Json
  action_url  String?   @db.VarChar(255)
  action_text String?   @db.VarChar(255)
  is_read     Boolean   @default(false)
  read_at     DateTime? @db.Timestamp(0)
  priority    String    @default("normal") @db.VarChar(255)
  expires_at  DateTime? @db.Timestamp(0)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  accounts    accounts  @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_notifications_user_id_foreign")

  @@index([priority], map: "student_notifications_priority_index")
  @@index([type], map: "student_notifications_type_index")
  @@index([user_id, created_at], map: "student_notifications_user_id_created_at_index")
  @@index([user_id, is_read], map: "student_notifications_user_id_is_read_index")
}

model student_parents_info {
  id           BigInt    @id(map: "idx_22394_primary") @default(autoincrement())
  fathers_name String?   @db.VarChar(255)
  mothers_name String?   @db.VarChar(255)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
}

model student_transactions {
  id             BigInt       @id(map: "idx_22403_primary") @default(autoincrement())
  student_id     BigInt
  transaction_id BigInt
  status         String       @db.VarChar(255)
  created_at     DateTime?    @db.Timestamptz(6)
  updated_at     DateTime?    @db.Timestamptz(6)
  amount         Int?
  transactions   transactions @relation(fields: [transaction_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "student_transactions_transaction_id_foreign")

  @@index([student_id], map: "idx_22403_student_transactions_student_id_foreign")
  @@index([transaction_id], map: "idx_22403_student_transactions_transaction_id_foreign")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model student_tuition {
  id                      BigInt    @id(map: "idx_22408_primary") @default(autoincrement())
  total_tuition           Float?    @db.Real
  total_balance           Float?    @db.Real
  total_lectures          Float?    @db.Real
  total_laboratory        Float?    @db.Real
  total_miscelaneous_fees Float?    @db.Real
  created_at              DateTime? @db.Timestamptz(6)
  updated_at              DateTime? @db.Date
  status                  String?   @db.VarChar(255)
  semester                BigInt?
  school_year             String?   @db.VarChar(255)
  academic_year           BigInt?
  student_id              BigInt?
  enrollment_id           BigInt?
  discount                BigInt?
  deleted_at              DateTime? @db.Timestamptz(6)
  overall_tuition         Float?    @db.Real
  paid                    BigInt?
  downpayment             Float?
  due_date                DateTime? @db.Date
  payment_method          String?   @db.VarChar(255)
  original_lectures       Float?
}

model students {
  id                      BigInt                     @id(map: "idx_22328_primary")
  first_name              String                     @db.VarChar(255)
  last_name               String                     @db.VarChar(255)
  middle_name             String?                    @db.VarChar(255)
  gender                  String                     @db.VarChar(255)
  birth_date              DateTime                   @db.Date
  age                     BigInt
  address                 String?                    @db.VarChar(255)
  contacts                String?
  course_id               BigInt
  academic_year           BigInt?
  email                   String?                    @db.VarChar(255)
  remarks                 String?
  created_at              DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at              DateTime                   @default(now()) @db.Timestamptz(6)
  profile_url             String?                    @db.VarChar(255)
  student_contact_id      BigInt?
  student_parent_info     BigInt?
  student_education_id    BigInt?
  student_personal_id     BigInt?
  document_location_id    BigInt?
  deleted_at              DateTime?                  @db.Timestamptz(6)
  student_id              BigInt?
  status                  String?                    @db.VarChar(255)
  clearance_status        students_clearance_status? @default(pending)
  year_graduated          String?                    @db.VarChar(255)
  special_order           String?                    @db.VarChar(255)
  issued_date             DateTime?                  @db.Date
  subject_enrolled        Json?                      @db.Json
  user_id                 Int?
  institution_id          Int?
  lrn                     String?                    @db.VarChar(255)
  student_type            String?                    @db.VarChar(255)
  ethnicity               String?                    @db.VarChar(255)
  city_of_origin          String?                    @db.VarChar(255)
  province_of_origin      String?                    @db.VarChar(255)
  region_of_origin        String?                    @db.VarChar(255)
  is_indigenous_person    Boolean                    @default(false)
  indigenous_group        String?                    @db.VarChar(255)
  withdrawal_date         DateTime?                  @db.Date
  withdrawal_reason       String?
  attrition_category      String?                    @db.VarChar(255)
  dropout_date            DateTime?                  @db.Date
  employment_status       String?                    @db.VarChar(255)
  employer_name           String?                    @db.VarChar(255)
  job_position            String?                    @db.VarChar(255)
  employment_date         DateTime?                  @db.Date
  employed_by_institution Boolean                    @default(false)
  scholarship_type        String?                    @db.VarChar(255)
  scholarship_details     String?
  student_clearances      student_clearances[]

  @@index([course_id], map: "idx_22328_fk_students_courses")
  @@index([student_contact_id], map: "idx_22328_fk_students_student_contacts")
  @@index([document_location_id], map: "idx_22328_idx_students_document_location_id")
  @@index([student_education_id], map: "idx_22328_idx_students_student_education_id")
  @@index([student_parent_info], map: "idx_22328_idx_students_student_parent_info")
  @@index([student_personal_id], map: "idx_22328_idx_students_student_personal_id")
  @@index([course_id], map: "idx_students_course_id")
  @@index([first_name, last_name], map: "idx_students_name_search")
  @@index([student_id], map: "idx_students_student_id")
}

model students_personal_info {
  id                BigInt    @id(map: "idx_22344_primary") @default(autoincrement())
  birthplace        String?   @db.VarChar(255)
  civil_status      String?   @db.VarChar(255)
  citizenship       String?   @db.VarChar(255)
  religion          String?   @db.VarChar(255)
  weight            String?   @db.VarChar(255)
  height            String?   @db.VarChar(255)
  current_adress    String?   @db.VarChar(255)
  permanent_address String?   @db.VarChar(255)
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
}

model subject {
  id             BigInt                 @id(map: "idx_22417_primary") @default(autoincrement())
  classification subject_classification @default(non_credited)
  code           String                 @db.VarChar(255)
  title          String                 @db.VarChar(255)
  units          BigInt?
  lecture        BigInt?
  laboratory     BigInt?
  pre_riquisite  String?
  academic_year  BigInt?
  semester       BigInt?
  course_id      BigInt?
  group          String?                @db.VarChar(255)
  is_credited    Boolean                @default(false)
  created_at     DateTime?              @db.Timestamp(0)
  updated_at     DateTime?              @db.Timestamp(0)
  description    String?                @db.VarChar(255)
  name           String?                @db.VarChar(255)
  classes        classes[]

  @@index([course_id], map: "idx_22417_fk_subject_courses")
}

model subject_enrollments {
  id                        BigInt    @id(map: "idx_22427_primary") @default(autoincrement())
  subject_id                BigInt?
  class_id                  Int?
  created_at                DateTime? @db.Timestamptz(6)
  updated_at                DateTime? @db.Timestamptz(6)
  grade                     Float?    @db.Real
  instructor                String?   @db.VarChar(255)
  student_id                Int?
  academic_year             String?   @db.VarChar(255)
  school_year               String?   @db.VarChar(255)
  semester                  Int?      @db.SmallInt
  enrollment_id             Int?      @db.SmallInt
  remarks                   String?   @db.VarChar(255)
  classification            String?   @db.VarChar(255)
  school_name               String?   @db.VarChar(255)
  is_credited               Boolean   @default(false)
  credited_subject_id       Int?      @db.SmallInt
  section                   String?   @db.VarChar(255)
  is_modular                Boolean   @default(false)
  lecture_fee               Decimal?  @db.Decimal(10, 2)
  laboratory_fee            Decimal?  @db.Decimal(10, 2)
  enrolled_lecture_units    Int?
  enrolled_laboratory_units Int?
  external_subject_code     String?   @db.VarChar(255)
  external_subject_title    String?   @db.VarChar(255)
  external_subject_units    Int?

  @@index([student_id], map: "idx_22427_fk_subject_enrollments_students")
  @@index([subject_id], map: "idx_22427_fk_subject_enrollments_subject")
  @@index([class_id, school_year, semester], map: "idx_subject_enrollments_class_semester")
  @@index([enrollment_id], map: "idx_subject_enrollments_enrollment_id")
  @@index([student_id, class_id, school_year, semester], map: "idx_subject_enrollments_transfer_lookup")
}

model table_cols {
  id                        BigInt    @id(map: "idx_22453_primary") @default(autoincrement())
  table_id                  BigInt
  name                      String    @db.VarChar(255)
  type                      String?   @default("varchar") @db.VarChar(255)
  length                    BigInt?   @default(255)
  default                   String?   @db.VarChar(255)
  comment                   String?   @db.VarChar(255)
  foreign_table             String?   @db.VarChar(255)
  foreign_col               String?   @db.VarChar(255)
  foreign_model             String?   @db.VarChar(255)
  nullable                  Boolean?  @default(false)
  index                     Boolean?  @default(false)
  auto_increment            Boolean?  @default(false)
  primary                   Boolean?  @default(false)
  unique                    Boolean?  @default(false)
  unsigned                  Boolean?  @default(false)
  foreign                   Boolean?  @default(false)
  foreign_on_delete_cascade Boolean?  @default(false)
  created_at                DateTime? @db.Timestamptz(6)
  updated_at                DateTime? @db.Timestamptz(6)

  @@index([table_id], map: "idx_22453_table_cols_table_id_foreign")
}

model tables {
  id           BigInt    @id(map: "idx_22441_primary") @default(autoincrement())
  module       String    @db.VarChar(255)
  name         String    @unique(map: "idx_22441_tables_name_unique") @db.VarChar(255)
  comment      String?   @db.VarChar(255)
  timestamps   Boolean?  @default(true)
  soft_deletes Boolean?  @default(false)
  migrated     Boolean?  @default(false)
  generated    Boolean?  @default(false)
  created_at   DateTime? @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)
}

model team_invitations {
  id         BigInt    @id(map: "idx_22480_primary") @default(autoincrement())
  team_id    BigInt
  email      String    @db.VarChar(255)
  role       String?   @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@unique([team_id, email], map: "idx_22480_team_invitations_team_id_email_unique")
}

model team_user {
  id         BigInt    @id(map: "idx_22488_primary") @default(autoincrement())
  team_id    BigInt
  user_id    BigInt
  role       String?   @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@unique([team_id, user_id], map: "idx_22488_team_user_team_id_user_id_unique")
}

model teams {
  id            BigInt     @id(map: "idx_22475_primary") @default(autoincrement())
  user_id       BigInt
  name          String     @db.VarChar(255)
  personal_team Boolean
  created_at    DateTime?  @db.Timestamptz(6)
  updated_at    DateTime?  @db.Timestamptz(6)
  accounts      accounts[]

  @@index([user_id], map: "idx_22475_teams_user_id_index")
}

model telescope_entries {
  sequence                BigInt                   @default(autoincrement())
  uuid                    String                   @unique(map: "telescope_entries_uuid_unique") @db.Uuid
  batch_id                String                   @db.Uuid
  family_hash             String?                  @db.VarChar(255)
  should_display_on_index Boolean                  @default(true)
  type                    String                   @db.VarChar(20)
  content                 String
  created_at              DateTime?                @db.Timestamp(0)
  telescope_entries_tags  telescope_entries_tags[]

  @@index([batch_id], map: "telescope_entries_batch_id_index")
  @@index([created_at], map: "telescope_entries_created_at_index")
  @@index([family_hash], map: "telescope_entries_family_hash_index")
  @@index([type, should_display_on_index], map: "telescope_entries_type_should_display_on_index_index")
}

model telescope_entries_tags {
  entry_uuid        String            @db.Uuid
  tag               String            @db.VarChar(255)
  telescope_entries telescope_entries @relation(fields: [entry_uuid], references: [uuid], onDelete: Cascade, onUpdate: NoAction, map: "telescope_entries_tags_entry_uuid_foreign")

  @@id([entry_uuid, tag])
  @@index([tag], map: "telescope_entries_tags_tag_index")
}

model telescope_monitoring {
  tag String @id @db.VarChar(255)
}

model testda_students {
  id         BigInt    @id(map: "idx_22511_primary") @default(autoincrement())
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model tickets {
  id          BigInt    @id(map: "idx_22516_primary") @default(autoincrement())
  uuid        String?   @db.Char(36)
  user_id     Decimal   @db.Decimal
  title       String    @db.VarChar(255)
  message     String?   @db.VarChar(255)
  priority    String    @default("low") @db.VarChar(255)
  status      String    @default("open") @db.VarChar(255)
  is_resolved Boolean   @default(false)
  is_locked   Boolean   @default(false)
  assigned_to Decimal?  @db.Decimal
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

model timesheets {
  id          BigInt    @id(map: "idx_22529_primary") @default(autoincrement())
  date        DateTime  @db.Date
  hours       Float
  description String?   @db.VarChar(255)
  project_id  Decimal   @db.Decimal
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)

  @@index([project_id], map: "idx_22529_timesheets_project_id_foreign")
}

model tracks_strands {
  id          BigInt    @id(map: "idx_22537_primary") @default(autoincrement())
  code        String    @db.VarChar(255)
  title       String    @db.VarChar(255)
  description String?   @db.VarChar(255)
  track_id    BigInt
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

model transactions {
  id                   BigInt                 @id(map: "idx_22545_primary") @default(autoincrement())
  transaction_number   String?                @unique(map: "idx_22545_idx_transaction_number") @db.VarChar(255)
  description          String                 @db.VarChar(255)
  status               String                 @db.VarChar(255)
  transaction_date     DateTime               @default(now()) @db.Timestamptz(6)
  created_at           DateTime?              @db.Timestamptz(6)
  updated_at           DateTime?              @db.Timestamptz(6)
  invoicenumber        String?                @db.VarChar(255)
  settlements          String?                @default("{\"registration_fee\":0,\"tuition_fee\":0,\"miscelanous_fee\":0,\"diploma_or_certificate\":0,\"transcript_of_records\":0,\"certification\":0,\"special_exam\":0,\"others\":0}")
  signature            String?
  payment_method       String?                @db.VarChar(255)
  user_id              BigInt?
  admin_transactions   admin_transactions[]
  student_transactions student_transactions[]
  users                users?                 @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "transactions_user_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model typables {
  type_id       Decimal @db.Decimal
  typables_id   Decimal @db.Decimal
  typables_type String  @db.VarChar(255)

  @@index([type_id], map: "idx_22555_typables_type_id_foreign")
  @@ignore
}

model types {
  id           BigInt    @id(map: "idx_22561_primary") @default(autoincrement())
  order        BigInt    @default(0)
  parent_id    Decimal?  @db.Decimal
  model_type   String?   @db.VarChar(255)
  model_id     Decimal?  @db.Decimal
  for          String?   @default("posts") @db.VarChar(255)
  type         String?   @default("category") @db.VarChar(255)
  name         String    @db.VarChar(255)
  key          String    @db.VarChar(255)
  description  String?
  color        String?   @db.VarChar(255)
  icon         String?   @db.VarChar(255)
  is_activated Boolean?  @default(true)
  created_at   DateTime? @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)

  @@index([parent_id], map: "idx_22561_types_parent_id_foreign")
}

model types_metas {
  id         BigInt    @id(map: "idx_22575_primary") @default(autoincrement())
  model_id   Decimal?  @db.Decimal
  model_type String?   @db.VarChar(255)
  type_id    Decimal   @db.Decimal
  key        String    @db.VarChar(255)
  value      String?
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)

  @@index([key], map: "idx_22575_types_metas_key_index")
  @@index([type_id], map: "idx_22575_types_metas_type_id_foreign")
}

model uptime_checks {
  id                               BigInt    @id(map: "idx_22583_primary") @default(autoincrement())
  look_for_string                  String    @default("") @db.VarChar(255)
  status                           String    @default("not yet checked") @db.VarChar(255)
  check_failure_reason             String?
  check_times_failed_in_a_row      BigInt    @default(0)
  status_last_change_date          DateTime? @db.Timestamptz(6)
  last_check_date                  DateTime? @db.Timestamptz(6)
  check_failed_event_fired_on_date DateTime? @db.Timestamptz(6)
  request_duration_ms              BigInt?
  check_method                     String    @default("get") @db.VarChar(255)
  check_payload                    String?
  check_additional_headers         String?
  check_response_checker           String?   @db.VarChar(255)
  site_id                          BigInt
  created_at                       DateTime? @db.Timestamptz(6)
  updated_at                       DateTime? @db.Timestamptz(6)

  @@index([site_id], map: "idx_22583_uptime_checks_site_id_foreign")
}

model user_activities {
  id         BigInt    @id(map: "idx_22613_primary") @default(autoincrement())
  user_id    BigInt?
  url        String    @db.VarChar(255)
  created_at DateTime? @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
}

model user_activity_logs {
  id                   BigInt   @id @default(autoincrement())
  user_id              BigInt
  activity_type        String   @db.VarChar(50)
  activity_description String
  subject_type         String?  @db.VarChar(255)
  subject_id           BigInt?
  properties           Json?    @db.Json
  ip_address           String?  @db.Inet
  user_agent           String?
  session_id           String?  @db.VarChar(255)
  url                  String?
  method               String?  @db.VarChar(10)
  status_code          Int?     @db.SmallInt
  response_time        Int?
  created_at           DateTime @db.Timestamp(0)
  users                users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_activity_logs_user_id_foreign")

  @@index([activity_type, created_at], map: "user_activity_logs_activity_type_created_at_index")
  @@index([activity_type], map: "user_activity_logs_activity_type_index")
  @@index([created_at], map: "user_activity_logs_created_at_index")
  @@index([ip_address, created_at], map: "user_activity_logs_ip_address_created_at_index")
  @@index([session_id], map: "user_activity_logs_session_id_index")
  @@index([subject_id], map: "user_activity_logs_subject_id_index")
  @@index([subject_type], map: "user_activity_logs_subject_type_index")
  @@index([subject_type, subject_id], map: "user_activity_logs_subject_type_subject_id_index")
  @@index([user_id, activity_type], map: "user_activity_logs_user_id_activity_type_index")
  @@index([user_id, created_at], map: "user_activity_logs_user_id_created_at_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model user_notification_preferences {
  id                BigInt    @id @default(autoincrement())
  user_id           BigInt
  notification_type String    @db.VarChar(255)
  channel           String    @db.VarChar(255)
  enabled           Boolean   @default(true)
  frequency         String    @default("immediate") @db.VarChar(255)
  quiet_hours_start DateTime? @db.Time(0)
  quiet_hours_end   DateTime? @db.Time(0)
  timezone          String    @default("UTC") @db.VarChar(255)
  settings          Json?     @db.Json
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  users             users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_notification_preferences_user_id_foreign")

  @@unique([user_id, notification_type, channel], map: "user_notification_preferences_user_id_notification_type_channel")
  @@index([enabled], map: "user_notification_preferences_enabled_index")
  @@index([frequency], map: "user_notification_preferences_frequency_index")
  @@index([notification_type, channel], map: "user_notification_preferences_notification_type_channel_index")
  @@index([user_id, channel], map: "user_notification_preferences_user_id_channel_index")
  @@index([user_id, notification_type], map: "user_notification_preferences_user_id_notification_type_index")
}

model user_sessions {
  id               BigInt    @id @default(autoincrement())
  user_id          BigInt
  session_id       String    @db.VarChar(255)
  ip_address       String    @db.Inet
  user_agent       String?
  login_at         DateTime  @db.Timestamp(0)
  logout_at        DateTime? @db.Timestamp(0)
  last_activity    DateTime  @db.Timestamp(0)
  is_active        Boolean   @default(true)
  location_country String?   @db.VarChar(255)
  location_region  String?   @db.VarChar(255)
  location_city    String?   @db.VarChar(255)
  device_type      String?   @db.VarChar(255)
  browser          String?   @db.VarChar(255)
  operating_system String?   @db.VarChar(255)
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  users            users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_sessions_user_id_foreign")

  @@index([ip_address, login_at], map: "user_sessions_ip_address_login_at_index")
  @@index([is_active], map: "user_sessions_is_active_index")
  @@index([last_activity], map: "user_sessions_last_activity_index")
  @@index([session_id], map: "user_sessions_session_id_index")
  @@index([session_id, is_active], map: "user_sessions_session_id_is_active_index")
  @@index([user_id, is_active], map: "user_sessions_user_id_is_active_index")
  @@index([user_id, login_at], map: "user_sessions_user_id_login_at_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model user_settings {
  id                            BigInt    @id @default(autoincrement())
  user_id                       BigInt    @unique(map: "user_settings_user_id_unique")
  semester                      Int?
  school_year_start             Int?
  created_at                    DateTime? @db.Timestamp(0)
  updated_at                    DateTime? @db.Timestamp(0)
  theme_preference              String    @default("system") @db.VarChar(255)
  language_preference           String    @default("en") @db.VarChar(5)
  timezone_preference           String    @default("UTC") @db.VarChar(255)
  email_notifications           Boolean   @default(true)
  push_notifications            Boolean   @default(true)
  sms_notifications             Boolean   @default(false)
  desktop_notifications         Boolean   @default(true)
  notification_frequency        String    @default("immediate") @db.VarChar(255)
  privacy_profile_visible       Boolean   @default(false)
  privacy_email_visible         Boolean   @default(false)
  privacy_phone_visible         Boolean   @default(false)
  privacy_show_online_status    Boolean   @default(true)
  privacy_allow_direct_messages Boolean   @default(true)
  privacy_show_in_directory     Boolean   @default(true)
  security_two_factor_enabled   Boolean   @default(false)
  security_session_timeout      Int       @default(120)
  security_login_alerts         Boolean   @default(true)
  dashboard_layout              String    @default("default") @db.VarChar(255)
  dashboard_widgets             Json?     @db.Json
  table_preferences             Json?     @db.Json
  sidebar_collapsed             Boolean   @default(false)
  items_per_page                Int       @default(25)
  date_format                   String    @default("Y-m-d") @db.VarChar(255)
  time_format                   String    @default("H:i:s") @db.VarChar(255)
  currency_format               String    @default("USD") @db.VarChar(255)
  number_format                 String    @default("en_US") @db.VarChar(255)
  custom_settings               Json?     @db.Json
  users                         users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_settings_user_id_foreign")

  @@index([email_notifications], map: "user_settings_email_notifications_index")
  @@index([language_preference], map: "user_settings_language_preference_index")
  @@index([theme_preference], map: "user_settings_theme_preference_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model users {
  id                                                           BigInt                          @id(map: "idx_22595_primary") @default(autoincrement())
  name                                                         String?                         @db.VarChar(255)
  email                                                        String                          @db.VarChar(255)
  email_verified_at                                            DateTime?                       @db.Timestamptz(6)
  password                                                     String?                         @db.VarChar(255)
  two_factor_secret                                            String?
  two_factor_recovery_codes                                    String?
  two_factor_confirmed_at                                      DateTime?                       @db.Timestamptz(6)
  remember_token                                               String?                         @db.VarChar(255)
  current_team_id                                              BigInt?
  avatar_url                                                   String?                         @db.VarChar(255)
  created_at                                                   DateTime?                       @db.Timestamptz(6)
  updated_at                                                   DateTime?                       @db.Timestamptz(6)
  deleted_at                                                   DateTime?                       @db.Timestamptz(6)
  theme                                                        String?                         @default("default") @db.VarChar(255)
  theme_color                                                  String?                         @db.VarChar(255)
  profile_photo_path                                           String?                         @db.VarChar(255)
  active_status                                                Boolean                         @default(false)
  avatar                                                       String                          @default("avatar.png") @db.VarChar(255)
  dark_mode                                                    Boolean                         @default(false)
  messenger_color                                              String?                         @db.VarChar(255)
  phone                                                        String?                         @db.VarChar(255)
  phone_verified_at                                            DateTime?                       @db.Timestamp(0)
  timezone                                                     String                          @default("UTC") @db.VarChar(255)
  language                                                     String                          @default("en") @db.VarChar(5)
  last_login_at                                                DateTime?                       @db.Timestamp(0)
  last_login_ip                                                String?                         @db.Inet
  two_factor_enabled                                           Boolean                         @default(false)
  login_attempts                                               Int                             @default(0) @db.SmallInt
  locked_until                                                 DateTime?                       @db.Timestamp(0)
  password_changed_at                                          DateTime?                       @db.Timestamp(0)
  must_change_password                                         Boolean                         @default(false)
  account_status                                               String                          @default("pending") @db.VarChar(255)
  profile_completed_at                                         DateTime?                       @db.Timestamp(0)
  date_of_birth                                                DateTime?                       @db.Date
  gender                                                       String?                         @db.VarChar(255)
  address                                                      String?
  city                                                         String?                         @db.VarChar(255)
  state                                                        String?                         @db.VarChar(255)
  country                                                      String?                         @db.VarChar(255)
  postal_code                                                  String?                         @db.VarChar(255)
  bio                                                          String?
  website                                                      String?                         @db.VarChar(255)
  social_links                                                 Json?                           @db.Json
  department                                                   String?                         @db.VarChar(255)
  position                                                     String?                         @db.VarChar(255)
  employee_id                                                  String?                         @unique(map: "users_employee_id_unique") @db.VarChar(255)
  hire_date                                                    DateTime?                       @db.Date
  manager_id                                                   BigInt?
  notification_preferences                                     Json?                           @db.Json
  privacy_settings                                             Json?                           @db.Json
  has_email_authentication                                     Boolean                         @default(false)
  role                                                         String                          @default("user") @db.VarChar(255)
  school_id                                                    BigInt?
  department_id                                                BigInt?
  app_authentication_secret                                    String?
  app_authentication_recovery_codes                            String?
  admin_transactions                                           admin_transactions[]
  announcements                                                announcements[]
  authenticator_apps                                           authenticator_apps[]
  bookings_bookings_approved_byTousers                         bookings[]                      @relation("bookings_approved_byTousers")
  bookings_bookings_booked_byTousers                           bookings[]                      @relation("bookings_booked_byTousers")
  bulk_actions                                                 bulk_actions[]
  class_enrollments                                            class_enrollments[]
  class_posts                                                  class_posts[]
  event_reminders                                              event_reminders[]
  event_rsvps                                                  event_rsvps[]
  events                                                       events[]
  export_jobs                                                  export_jobs[]
  exports                                                      exports[]
  failed_import_rows                                           failed_import_rows[]
  fblog_posts                                                  fblog_posts[]
  inventory_amendments_inventory_amendments_amended_byTousers  inventory_amendments[]          @relation("inventory_amendments_amended_byTousers")
  inventory_amendments_inventory_amendments_approved_byTousers inventory_amendments[]          @relation("inventory_amendments_approved_byTousers")
  inventory_borrowings_inventory_borrowings_issued_byTousers   inventory_borrowings[]          @relation("inventory_borrowings_issued_byTousers")
  inventory_borrowings_inventory_borrowings_returned_toTousers inventory_borrowings[]          @relation("inventory_borrowings_returned_toTousers")
  inventory_stock_movements                                    inventory_stock_movements[]
  library_borrow_records                                       library_borrow_records[]
  passkeys                                                     passkeys[]
  pending_enrollments                                          pending_enrollments[]
  resource_bookings                                            resource_bookings[]
  route_statistics                                             route_statistics[]
  transactions                                                 transactions[]
  user_activity_logs                                           user_activity_logs[]
  user_notification_preferences                                user_notification_preferences[]
  user_sessions                                                user_sessions[]
  user_settings                                                user_settings?
  departments                                                  departments?                    @relation(fields: [department_id], references: [id], onUpdate: NoAction, map: "users_department_id_foreign")
  users                                                        users?                          @relation("usersTousers", fields: [manager_id], references: [id], onUpdate: NoAction, map: "users_manager_id_foreign")
  other_users                                                  users[]                         @relation("usersTousers")
  schools                                                      schools?                        @relation(fields: [school_id], references: [id], onUpdate: NoAction, map: "users_school_id_foreign")

  @@index([account_status], map: "users_account_status_index")
  @@index([department_id, role], map: "users_department_id_role_index")
  @@index([department], map: "users_department_index")
  @@index([employee_id], map: "users_employee_id_index")
  @@index([last_login_at], map: "users_last_login_at_index")
  @@index([phone], map: "users_phone_index")
  @@index([school_id, department_id, role], map: "users_school_id_department_id_role_index")
  @@index([school_id, role], map: "users_school_id_role_index")
}

model versions {
  id               BigInt    @id @default(autoincrement())
  user_id          BigInt?
  versionable_type String    @db.VarChar(255)
  versionable_id   BigInt
  contents         Json?     @db.Json
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  deleted_at       DateTime? @db.Timestamp(0)

  @@index([versionable_type, versionable_id], map: "versions_versionable_type_versionable_id_index")
}

model volet_feature_comments {
  id             BigInt         @id @default(autoincrement())
  feature_id     BigInt
  author_id      String         @db.VarChar(255)
  content        String
  created_at     DateTime?      @db.Timestamp(0)
  updated_at     DateTime?      @db.Timestamp(0)
  volet_features volet_features @relation(fields: [feature_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "volet_feature_comments_feature_id_foreign")
}

model volet_feature_user_votes {
  id             BigInt         @id @default(autoincrement())
  feature_id     BigInt
  author_id      String         @db.VarChar(255)
  created_at     DateTime?      @db.Timestamp(0)
  updated_at     DateTime?      @db.Timestamp(0)
  volet_features volet_features @relation(fields: [feature_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "volet_feature_user_votes_feature_id_foreign")

  @@unique([feature_id, author_id], map: "volet_feature_user_votes_feature_id_author_id_unique")
}

model volet_features {
  id                       BigInt                     @id @default(autoincrement())
  title                    String                     @db.VarChar(255)
  description              String
  category                 String                     @db.VarChar(255)
  status                   String                     @default("pending") @db.VarChar(255)
  author_id                String                     @db.VarChar(255)
  created_at               DateTime?                  @db.Timestamp(0)
  updated_at               DateTime?                  @db.Timestamp(0)
  volet_feature_comments   volet_feature_comments[]
  volet_feature_user_votes volet_feature_user_votes[]

  @@index([category], map: "volet_features_category_index")
  @@index([status], map: "volet_features_status_index")
}

model volet_feedback_messages {
  id         BigInt    @id @default(autoincrement())
  message    String
  status     String    @default("new") @db.VarChar(255)
  category   String    @db.VarChar(255)
  user_info  Json?     @db.Json
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@index([category], map: "volet_feedback_messages_category_index")
  @@index([status], map: "volet_feedback_messages_status_index")
}

model webauthn_keys {
  id              BigInt    @id(map: "idx_22618_primary") @default(autoincrement())
  user_id         BigInt
  name            String?   @db.VarChar(255)
  credential_id   String
  public_key      String
  attachment_type String?   @db.VarChar(50)
  is_passkey      Boolean   @default(false)
  transports      String?
  last_used_at    DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
}

enum faculty_gender {
  male
  female
  other
}

enum faculty_status {
  active
  inactive
  on_leave
  retired
}

enum fblog_posts_status {
  published
  scheduled
  pending
}

enum process_approval_flow_steps_action {
  APPROVE
  VERIFY
  CHECK
}

enum students_clearance_status {
  pending
  approved
  rejected
}

enum subject_classification {
  credited
  non_credited
  internal
}
